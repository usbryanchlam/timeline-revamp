---
phase: 11-mp4-rung-2-3-or-mobile-polish-a11y-audit
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .planning/REQUIREMENTS.md
  - src/reel/Reel.tsx
  - src/reel/Reel.keyboard.test.tsx
  - src/gestures/useGestureMachine.ts
  - src/gestures/stateMachine.ts
  - src/gestures/stateMachine.test.ts
  - src/routes/TripsRoute.a11y.test.tsx
  - src/auth/suggestHandle.test.ts
  - src/auth/HandlePickerGate.test.tsx
  - src/dev/useFrameRate.ts
  - src/dev/useFrameRate.test.ts
  - src/dev/FpsBadge.tsx
  - src/reel/OrbitReel.tsx
  - scripts/lighthouse-baseline.sh
  - scripts/assert-lighthouse-thresholds.ts
  - package.json
autonomous: true
requirements: [A11Y-02, A11Y-08]
tags: [a11y, keyboard, fps, lighthouse, perf, f8, suggestHandle, tree-shake]

must_haves:
  truths:
    - "REQUIREMENTS.md A11Y-08 line reflects shipped semantics: ←/→ chapter prev/next (NOT scrub ±1s); Phase 11 edits the line per D-LOCK"
    - "Keyboard-only path completes 'add a city' flow from /app/trips end-to-end without mouse — A11Y-02 closed"
    - "Reel keyboard handlers cover Space (toggle paused), ↑/↓ (chapter prev/next), ←/→ (chapter prev/next per locked semantics), Enter (open photo detail) — A11Y-08 closed"
    - "src/auth/suggestHandle.ts has tests covering nickname → email-local → given_name → empty fallback chain + sanitize against reserved-word list + 20-char truncate — F8 closed"
    - "src/dev/useFrameRate.ts + FpsBadge.tsx provide median + p95 FPS readout gated on import.meta.env.DEV && ?fps=1; tree-shaken from production bundle (build-time grep guard)"
    - "Lighthouse baseline produced: docs/lighthouse/v1.0.0-baseline.json + .html with perf ≥ 90, LCP ≤ 2.5s, CLS ≤ 0.1 (PERF-01/03/04 evidence)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "A11Y-08 line aligned with shipped ←/→ JUMP_CHAPTER semantics"
      contains: "A11Y-08"
    - path: "src/reel/Reel.keyboard.test.tsx"
      provides: "Vitest coverage of Space + Arrows + Enter handlers"
      contains: "keyboard"
    - path: "src/routes/TripsRoute.a11y.test.tsx"
      provides: "userEvent.keyboard add-city flow"
      contains: "userEvent"
    - path: "src/auth/suggestHandle.test.ts"
      provides: "Algorithm coverage: nickname/email/given_name + sanitize + reserved-word filter"
      contains: "suggestHandle"
    - path: "src/dev/useFrameRate.ts"
      provides: "rAF-delta sampler, DEV-gated, returns median + p95 over windowMs"
      contains: "import.meta.env.DEV"
    - path: "src/dev/FpsBadge.tsx"
      provides: "Amber readout for FPS hook, DEV-gated, gated on ?fps=1"
      contains: "useFrameRate"
    - path: "scripts/lighthouse-baseline.sh"
      provides: "Preview server + Lighthouse mobile run + symlinked JSON/HTML"
      contains: "bun run preview"
    - path: "scripts/assert-lighthouse-thresholds.ts"
      provides: "Parses baseline JSON, exits non-zero on perf < 90 OR LCP > 2500 OR CLS > 0.1"
      contains: "performance.score"
    - path: "docs/lighthouse/v1.0.0-baseline.json"
      provides: "PERF-01/03/04 evidence artifact for v1.0.0 launch"
      contains: "categories"
  key_links:
    - from: "src/reel/Reel.tsx"
      to: "Enter → open photo detail sheet"
      via: "keyboard handler dispatches OPEN_DETAIL"
      pattern: "key.*Enter"
    - from: "src/dev/FpsBadge.tsx"
      to: "src/reel/OrbitReel.tsx"
      via: "{import.meta.env.DEV && <FpsBadge />} conditional render"
      pattern: "import\\.meta\\.env\\.DEV.*FpsBadge"
    - from: "src/auth/HandlePickerGate.tsx"
      to: "src/auth/suggestHandle.ts"
      via: "suggestHandle(user) → HandlePickerModal suggestedHandle prop"
      pattern: "suggestHandle"
    - from: "scripts/lighthouse-baseline.sh"
      to: "docs/lighthouse/v1.0.0-baseline.{json,html}"
      via: "lighthouse --output=json --output=html with symlink"
      pattern: "v1\\.0\\.0-baseline"
---

<objective>
Close the keyboard a11y requirements (A11Y-02 + A11Y-08), add tests for the already-on-disk F8 pre-fill algorithm, add a dev-only FPS instrumentation hook with build-time tree-shake guard, and produce the v1.0.0 Lighthouse mobile baseline.

Purpose: This plan rolls up the four "behavior + measurement" triage items not yet covered by 11-01. A11Y-08 has a REQUIREMENTS.md edit per D-LOCK (semantics align with shipped code). The FPS hook + Lighthouse baseline gate PERF-01/03/04 evidence for the v1.0.0 launch.

Output:
- REQUIREMENTS.md line 31 edited to drop `←/→ scrub ±1s` and reflect shipped `←/→ chapter prev/next`
- Reel.tsx + useGestureMachine.ts extended with Enter → OPEN_DETAIL handler
- Tests for keyboard add-city flow + Reel keyboard handlers + suggestHandle + HandlePickerGate
- src/dev/{useFrameRate.ts, useFrameRate.test.ts, FpsBadge.tsx} + OrbitReel.tsx renders FpsBadge under DEV+?fps=1 gate
- scripts/lighthouse-baseline.sh + scripts/assert-lighthouse-thresholds.ts + docs/lighthouse/v1.0.0-baseline.{json,html}
- Tree-shake guard: ! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-RESEARCH.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-VALIDATION.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-01-PLAN.md
@DESIGN.md
@~/.claude/projects/-Users-bryanlam-Workspaces-timeline-revamp/memory/feedback_grep_guard_vs_comments.md
@~/.claude/projects/-Users-bryanlam-Workspaces-timeline-revamp/memory/feedback_mountedref_strictmode.md
@src/reel/Reel.tsx
@src/reel/OrbitReel.tsx
@src/gestures/useGestureMachine.ts
@src/gestures/stateMachine.ts
@src/routes/TripsRoute.tsx
@src/auth/suggestHandle.ts
@src/auth/HandlePickerGate.tsx
@src/auth/HandlePickerModal.tsx
@server/handles/validate.ts

<interfaces>
<!-- Reference patterns the executor needs verbatim. -->

src/auth/suggestHandle.ts (already on disk — Phase 11 adds tests, NOT algorithm):
```ts
// Sanitize chain (already shipped):
function sanitize(raw: string): string {
  const lowercased = raw.toLowerCase();
  const dashed = lowercased.replace(/[._\s]+/g, '-');
  const stripped = dashed.replace(/[^a-z0-9-]/g, '');
  const collapsed = stripped.replace(/-+/g, '-');
  const trimmed = collapsed.replace(/^-+|-+$/g, '');
  if (trimmed.length === 0) return '';
  const truncated = trimmed.slice(0, 20);
  return truncated.replace(/-+$/, '');
}
// Fallback chain: nickname → email local-part → given_name → ''
// Final filter: validateHandle() from @server/handles/validate (single source of truth — 26-word reserved set + [a-z0-9-]{3,20} regex)
```

src/dev/useFrameRate.ts (NEW — design per RESEARCH.md Pattern 3):
```ts
import { useEffect, useRef, useState } from 'react';
interface Sample { fps: number; median: number; p95: number; sampleCount: number; }
export function useFrameRate(opts?: { windowMs?: number; enabled?: boolean }): Sample | null {
  const enabled = (opts?.enabled ?? true)
    && import.meta.env.DEV
    && (typeof location !== 'undefined' && new URLSearchParams(location.search).has('fps'));
  // ... rAF sampler ...
}
```

DEV gate (Vite replaces `import.meta.env.DEV` literal with `false` at build, dead-coding the body):
```tsx
{import.meta.env.DEV && <FpsBadge />}
```

A11Y-08 REQUIREMENTS.md edit (per D-LOCK CONTEXT.md):
Current line 31: `**A11Y-08**: Keyboard controls: ←/→ scrub ±1s, ↑/↓ chapter prev/next, Space play/pause, Enter open detail.`
Replace with: `**A11Y-08**: Keyboard controls: ←/→ chapter prev/next, ↑/↓ chapter prev/next, Space play/pause, Enter open detail.`
(Rationale: shipped semantics — gesture machine uses JUMP_CHAPTER on Arrow*; no scrub state-machine event.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A11Y-02 + A11Y-08 — REQUIREMENTS.md edit, keyboard handlers (Enter → OPEN_DETAIL), keyboard tests</name>
  <read_first>
    - .planning/REQUIREMENTS.md (line 31 — current A11Y-08 text)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md (D-LOCK on A11Y-08 semantics: keep current JUMP_CHAPTER)
    - src/gestures/useGestureMachine.ts (lines ~261-274: Space + Arrow handlers, missing Enter)
    - src/gestures/stateMachine.ts (event union — may need OPEN_DETAIL added)
    - src/reel/Reel.tsx (where detail sheet open is dispatched)
    - src/routes/TripsRoute.tsx + src/components/MapPicker.tsx + src/components/CityForm.tsx (add-city flow surfaces)
  </read_first>
  <files>
.planning/REQUIREMENTS.md,
src/gestures/useGestureMachine.ts,
src/gestures/stateMachine.ts,
src/gestures/stateMachine.test.ts,
src/reel/Reel.tsx,
src/reel/Reel.keyboard.test.tsx,
src/routes/TripsRoute.a11y.test.tsx
  </files>
  <behavior>
- Test 1 (A11Y-08): Pressing Space on focused Reel container dispatches TOGGLE_PAUSED — state toggles between IDLE and PAUSED
- Test 2 (A11Y-08): Pressing ArrowUp / ArrowLeft dispatches JUMP_CHAPTER prev (chapterIndex decrements by 1, clamped at 0)
- Test 3 (A11Y-08): Pressing ArrowDown / ArrowRight dispatches JUMP_CHAPTER next (chapterIndex increments by 1, clamped at chapters.length-1)
- Test 4 (A11Y-08): Pressing Enter on focused Reel container opens the photo detail sheet for the current chapter's first photo (or no-op if no photos)
- Test 5 (A11Y-02): userEvent.keyboard sequence on TripsRoute reaches "add a city" save without any user.click — Tab to MapPicker, Enter to commit pick, fill name + arrived date via keyboard, Tab to Save, Enter — assert new city row visible
- Test 6: REQUIREMENTS.md A11Y-08 line matches shipped semantics (grep guard)
  </behavior>
  <action>
**RED: Write tests first**

1. Edit `.planning/REQUIREMENTS.md` line 31 per D-LOCK CONTEXT decision. Replace `←/→ scrub ±1s` with `←/→ chapter prev/next`. EXACT new line:
   ```
   - [ ] **A11Y-08**: Keyboard controls: ←/→ chapter prev/next, ↑/↓ chapter prev/next, Space play/pause, Enter open detail.
   ```
   Verify: `grep -q '←/→ chapter prev/next' .planning/REQUIREMENTS.md && ! grep -q '←/→ scrub' .planning/REQUIREMENTS.md`.

2. Create `src/reel/Reel.keyboard.test.tsx` with Tests 1-4 above. Use `userEvent.keyboard()` and focus the Reel container with `tabIndex={-1}` or by document.activeElement. If Reel currently consumes useGestureMachine internally without a way to read state, mock useGestureMachine and assert the dispatched event names.

3. Extend `src/gestures/stateMachine.test.ts` with a case asserting `transition(state, { type: 'OPEN_DETAIL' })` produces a state with `detailOpen: true` for chapterIndex (or whatever shape matches the existing pattern after reading the actual file).

4. Create `src/routes/TripsRoute.a11y.test.tsx` with Test 5 — full keyboard add-city flow with userEvent.keyboard(). Mock MapPicker click via keyboard Enter on a focused map element OR (if MapPicker lacks keyboard support) note that A11Y-02 currently requires a MapPicker keyboard affordance — add the affordance if missing (a focusable button labeled "Add city at current map center" that triggers MapPicker.onPick at the visible map center). The exact path is at executor's discretion within the keyboard-only constraint.

Run tests now. All must FAIL (RED).

**GREEN: Implement**

5. Edit `src/gestures/stateMachine.ts` — add `OPEN_DETAIL` to the event union if absent; add a transition that sets some `detailOpen: true` flag (or fires a callback — match existing pattern after reading the file). If the state machine doesn't own detail-sheet state, add a callback prop to useGestureMachine instead.

6. Edit `src/gestures/useGestureMachine.ts` keyboard handler (lines ~261-274). Add Enter → dispatch OPEN_DETAIL (or invoke a passed-in onOpenDetail callback). Existing Space + Arrow handlers stay verbatim (D-LOCK).

7. Edit `src/reel/Reel.tsx` to subscribe to the detail-open signal — either reading state.detailOpen or passing an onOpenDetail handler. Open the existing PhotoDetailSheet for chapters[state.chapterIndex] (use the first photo, or current photo if PhotoCycle exposes one).

8. If TripsRoute test required a keyboard-affordance for MapPicker, add it (a focusable button or aria-label on the map element). Use amber-accent CSS classes only (DESIGN.md).

Run tests — all 6 must pass (GREEN).

**Grep-guard hygiene** (per memory feedback_grep_guard_vs_comments.md): the REQUIREMENTS.md edit task uses a grep that targets the exact arrow phrase; since REQUIREMENTS.md is a markdown file with no `#` comments at line start, the grep is safe. Do NOT paraphrase the new A11Y-08 text in a comment elsewhere.
  </action>
  <verify>
    <automated>grep -q '←/→ chapter prev/next' .planning/REQUIREMENTS.md && ! grep -q '←/→ scrub' .planning/REQUIREMENTS.md && bun test src/reel/Reel.keyboard.test.tsx src/gestures/stateMachine.test.ts src/routes/TripsRoute.a11y.test.tsx 2>&1 | tail -5 | grep -qE 'pass|passed'</automated>
  </verify>
  <acceptance_criteria>
- `grep -c '←/→ chapter prev/next' .planning/REQUIREMENTS.md` outputs at least `1`
- `! grep -q '←/→ scrub' .planning/REQUIREMENTS.md` (old text gone)
- `grep -E "OPEN_DETAIL|onOpenDetail" src/gestures/stateMachine.ts` matches (event added)
- `grep -E "key.*Enter|'Enter'" src/gestures/useGestureMachine.ts` matches (handler added)
- `bun test src/reel/Reel.keyboard.test.tsx` exits 0
- `bun test src/gestures/stateMachine.test.ts` exits 0 (no regression + new OPEN_DETAIL coverage)
- `bun test src/routes/TripsRoute.a11y.test.tsx` exits 0
- A11Y-02 hard gate: full keyboard sequence in TripsRoute test contains zero `user.click(` calls — verify with `! grep -q 'user\.click(' src/routes/TripsRoute.a11y.test.tsx` (single negation; exits 0 only if the literal is absent)
  </acceptance_criteria>
  <done>
A11Y-02 closed (keyboard-only add-city flow tested + any missing affordance added). A11Y-08 closed (REQUIREMENTS.md edited to shipped semantics; Enter handler added for OPEN_DETAIL). Existing arrow + Space behavior preserved per D-LOCK.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F8 — suggestHandle tests + HandlePickerGate integration test (no algorithm change)</name>
  <read_first>
    - src/auth/suggestHandle.ts (algorithm is ALREADY on disk — per D-LOCK, Phase 11 adds tests + UAT verification, NOT the algorithm; if file is missing, halt and report)
    - src/auth/HandlePickerGate.tsx (wires suggestHandle(user) → HandlePickerModal suggestedHandle prop)
    - src/auth/HandlePickerModal.tsx (consumes suggestedHandle prop)
    - server/handles/validate.ts (single source of truth for validation — 26-entry reserved set + regex)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md (F8 acceptance criteria)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-RESEARCH.md Pattern 5
  </read_first>
  <files>
src/auth/suggestHandle.test.ts,
src/auth/HandlePickerGate.test.tsx
  </files>
  <behavior>
- Test 1: suggestHandle({ nickname: 'Bryan Lam' }) → returns 'bryan-lam' (space → dash, lowercase)
- Test 2: suggestHandle({ nickname: 'Admin' }) → returns '' (reserved-word filter via validateHandle)
- Test 3: suggestHandle({ email: 'bryan.lam@gmail.com' }) (no nickname) → returns 'bryan-lam' (email local-part fallback, dots → dashes)
- Test 4: suggestHandle({ given_name: 'Bryan' }) (no nickname, no email) → returns 'bryan' (given_name fallback)
- Test 5: suggestHandle({}) → returns '' (no source claims → empty)
- Test 6: suggestHandle({ nickname: 'Très Bién!!!' }) → returns 'tr-s-bi-n' or similar — non-ASCII stripped, multiple dashes collapsed, trailing dashes removed (asserts sanitize chain correctness)
- Test 7: suggestHandle({ nickname: 'a'.repeat(50) }) → returns 'aaaaaaaaaaaaaaaaaaaa' (exactly 20 chars — truncate works)
- Test 8: suggestHandle({ nickname: 'ab' }) → returns '' (failed minimum-length 3 via validateHandle)
- Test 9 (HandlePickerGate integration): When useAuth0 returns a user with nickname='Bryan Lam' AND server /api/me returns handle=null, HandlePickerModal renders with input value 'bryan-lam'
- Test 10 (HandlePickerGate integration): When suggestion is empty (no clean fallback), HandlePickerModal renders with empty input value
  </behavior>
  <action>
**Verify the algorithm is on disk first.** `ls src/auth/suggestHandle.ts` must succeed. If absent, halt — D-LOCK says algorithm exists; this is a divergence from CONTEXT and must be reported, not silently implemented.

**RED: Write tests**

1. Create `src/auth/suggestHandle.test.ts` with Tests 1-8 above. Import `suggestHandle` (named export). For tests that depend on the 26-entry reserved-word list and regex, do NOT inline duplicate that list — they live in `server/handles/validate.ts`. The tests assert outputs only.

2. Create `src/auth/HandlePickerGate.test.tsx` with Tests 9-10. Mock `useAuth0()` (vi.mock) to return the desired user; mock the `/api/me` fetch to return `{ handle: null }`. Render HandlePickerGate; assert HandlePickerModal mounts with the expected suggestedHandle prop reflected in the input value.

Run tests. Some may PASS on disk already (algorithm is shipped); the ones that fail expose actual gaps in the algorithm or wiring — flag in SUMMARY.

**GREEN: Only fix wiring, NOT the algorithm**

3. Per D-LOCK: the algorithm in `src/auth/suggestHandle.ts` is shipped. If a unit test of the algorithm fails, this is a finding — record in SUMMARY but do NOT modify the algorithm without orchestrator approval.

4. If HandlePickerGate integration tests fail because the wiring is incomplete (e.g., suggestion not passed through), fix the wiring in HandlePickerGate.tsx — that is wiring, not algorithm.

5. Re-verify the existing 26-entry reserved-word list is applied by reading `server/handles/validate.ts` and counting. `grep -cE "^\s*'[a-z]+'" server/handles/validate.ts` should match the count (or use the structure that's actually on disk).

**UAT note for SUMMARY**: After this task, manually verify F8 in a fresh Auth0 login (delete `users.handle` for the test user, log in, observe HandlePickerModal pre-fills the input). Record outcome in SUMMARY.

Per D-LOCK CONTEXT note 6: algorithm is shipped — Phase 11 tests + UAT verification only.
  </action>
  <verify>
    <automated>test -f src/auth/suggestHandle.ts && bun test src/auth/suggestHandle.test.ts src/auth/HandlePickerGate.test.tsx 2>&1 | tail -5 | grep -qE 'pass|passed'</automated>
  </verify>
  <acceptance_criteria>
- `test -f src/auth/suggestHandle.ts` succeeds (algorithm present on disk — D-LOCK precondition)
- `test -f src/auth/suggestHandle.test.ts` succeeds (NEW)
- `test -f src/auth/HandlePickerGate.test.tsx` succeeds (NEW)
- `bun test src/auth/suggestHandle.test.ts` exits 0 (all 8 algorithm cases pass — OR test file outputs a clear failure mapping to a specific finding to record in SUMMARY)
- `bun test src/auth/HandlePickerGate.test.tsx` exits 0 (integration: gate passes suggestion to modal)
- `git diff src/auth/suggestHandle.ts` outputs nothing (algorithm unchanged per D-LOCK)
- Existing HandlePickerModal.test.tsx still green: `bun test src/auth/HandlePickerModal.test.tsx` exits 0
  </acceptance_criteria>
  <done>
suggestHandle algorithm covered by tests (8 cases incl. reserved-word filter + truncate + minimum-length); HandlePickerGate integration verified end-to-end (test). Algorithm file untouched per D-LOCK.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FPS instrumentation — useFrameRate + FpsBadge (DEV+?fps=1 gated) + tree-shake guard against dist/</name>
  <read_first>
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-RESEARCH.md Pattern 3 + Pitfall 6 (tree-shake)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md (D-LOCK 7: ?fps=1 query param precedent from ?signup=1)
    - src/reel/OrbitReel.tsx (where FpsBadge mounts — the 8s orbit closes Phase 7 UAT #1)
    - DESIGN.md (single amber accent — FpsBadge text uses amber-500 dark / amber-600 light)
    - ~/.claude/projects/-Users-bryanlam-Workspaces-timeline-revamp/memory/feedback_grep_guard_vs_comments.md (grep-vs-comments hazard for the tree-shake assertion)
    - vite.config.ts (current build output path → `dist/`)
  </read_first>
  <files>
src/dev/useFrameRate.ts,
src/dev/useFrameRate.test.ts,
src/dev/FpsBadge.tsx,
src/reel/OrbitReel.tsx,
package.json
  </files>
  <behavior>
- Test 1: When `import.meta.env.DEV=false`, useFrameRate returns null and starts no rAF loop (enabled-flag chain)
- Test 2: When DEV=true but URLSearchParams has no 'fps' key, useFrameRate returns null (gated on ?fps=1)
- Test 3: When DEV=true and ?fps=1 present, useFrameRate starts sampling and after windowMs returns { fps, median, p95, sampleCount } with median computed as sorted[Math.floor(n/2)] and p95 computed at index Math.floor(n*0.05) (5th-percentile-low per RESEARCH.md)
- Test 4: useFrameRate cleanup cancels the rAF on unmount (no leak)
- Test 5: Build-time tree-shake guarantee: after `bun run build`, `! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js` succeeds (zero matches → hook + badge dropped from prod bundle)
  </behavior>
  <action>
**RED: Write tests first**

1. Create `src/dev/useFrameRate.test.ts` covering Tests 1-4. Use vitest's `vi.stubGlobal` to mock `location.search` for the ?fps=1 cases; use `vi.useFakeTimers` + `vi.stubGlobal('performance', { now: () => fakeTime })` and a mocked requestAnimationFrame to advance through samples. Assert median + p95 math against a known sample distribution (e.g., 60 frames at 16.67ms delta → median 60 fps, p95-low 60 fps).

Run — must FAIL (RED) since files do not yet exist.

**GREEN: Implement**

2. Create `src/dev/useFrameRate.ts` per RESEARCH.md Pattern 3 (full source in 11-RESEARCH.md lines ~300-348). EXACT structure:
   ```ts
   import { useEffect, useRef, useState } from 'react';
   interface Sample { fps: number; median: number; p95: number; sampleCount: number; }
   export function useFrameRate(opts?: { windowMs?: number; enabled?: boolean }): Sample | null {
     const enabled = (opts?.enabled ?? true)
       && import.meta.env.DEV
       && (typeof location !== 'undefined' && new URLSearchParams(location.search).has('fps'));
     const windowMs = opts?.windowMs ?? 8000;
     // ... rAF sampler, sort, return Sample
   }
   ```
   (Use the exact code shown in RESEARCH.md Pattern 3. Re-anchor any mutable refs inside the effect per feedback_mountedref_strictmode.md if needed.)

3. Create `src/dev/FpsBadge.tsx`:
   ```tsx
   import { useFrameRate } from './useFrameRate';
   export function FpsBadge() {
     if (!import.meta.env.DEV) return null;
     const s = useFrameRate();
     if (!s) return null;
     return (
       <div
         aria-hidden="true"
         className="fixed top-2 left-2 z-[100] rounded-md bg-bg-elev/80 px-2 py-1 text-[10px] text-amber-400 tabular-nums backdrop-blur"
       >
         {s.fps} fps · med {s.median} · p95-low {s.p95} · n={s.sampleCount}
       </div>
     );
   }
   ```
   (Amber-only per DESIGN.md single-accent rule. NO other colors.)

4. Mount `<FpsBadge />` inside OrbitReel where the 8s orbit runs. EXACT wiring (the DEV literal is essential for tree-shake):
   ```tsx
   import { FpsBadge } from '@/dev/FpsBadge';
   // ... inside JSX:
   {import.meta.env.DEV && <FpsBadge />}
   ```
   Place at the top-left of the orbit-active surface (so the iPhone Safari Web Inspector or naked eye reads it during UAT).

5. Run vitest tests — all 4 pass (GREEN).

6. **Tree-shake guard** (RED → GREEN for the build-time gate):
   - Run `bun run build`.
   - Assert `! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js` (zero files containing either symbol in the built bundle).
   - Per memory feedback_grep_guard_vs_comments.md: this is a built-output grep, not a source-comment grep, so it's safe. The build emits minified JS only; if the symbols leak, the DEV gate is broken.
   - If a match is found, the gate failed: check (a) the conditional uses `import.meta.env.DEV` literal (not a destructured variable), (b) FpsBadge is not imported unconditionally somewhere else, (c) `vite build` minifier dead-code-eliminates the branch.

7. Add a package.json script `"verify:tree-shake": "bun run build && ! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js"`. Wire so CI / phase verify can run it.

Per D-LOCK CONTEXT note 7: ?fps=1 precedent matches Phase 8's ?signup=1.
  </action>
  <verify>
    <automated>bun test src/dev/useFrameRate.test.ts 2>&1 | tail -5 | grep -qE 'pass|passed' && grep -q 'import.meta.env.DEV' src/dev/useFrameRate.ts && grep -q 'import.meta.env.DEV' src/dev/FpsBadge.tsx && grep -q 'import.meta.env.DEV.*FpsBadge\|FpsBadge.*import.meta.env.DEV' src/reel/OrbitReel.tsx && bun run build && ! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js</automated>
  </verify>
  <acceptance_criteria>
- `test -f src/dev/useFrameRate.ts && test -f src/dev/FpsBadge.tsx && test -f src/dev/useFrameRate.test.ts`
- `grep -c 'import.meta.env.DEV' src/dev/useFrameRate.ts` outputs at least `1`
- `grep -c 'import.meta.env.DEV' src/dev/FpsBadge.tsx` outputs at least `1`
- `grep -c "URLSearchParams.*has.'fps'" src/dev/useFrameRate.ts` outputs at least `1`
- `grep -E 'import.meta.env.DEV.*FpsBadge|FpsBadge.*import.meta.env.DEV' src/reel/OrbitReel.tsx` matches (conditional render in OrbitReel)
- `bun test src/dev/useFrameRate.test.ts` exits 0
- After `bun run build`: `! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js` succeeds (zero matches in built bundle — TREE-SHAKE GUARD)
- `jq -r '.scripts["verify:tree-shake"]' package.json` outputs the verify command containing both `bun run build` and the grep negation
- DESIGN invariant: `! grep -E 'bg-(blue|red|green|purple|pink|cyan|indigo|teal)-[0-9]' src/dev/FpsBadge.tsx` (amber only)
- No non-amber colors in OrbitReel diff: `! git diff src/reel/OrbitReel.tsx | grep -E 'bg-(blue|red|green|purple|pink|cyan|indigo|teal)-[0-9]'`
  </acceptance_criteria>
  <done>
FPS hook + badge live under src/dev/, DEV+?fps=1 gated, amber-only per DESIGN.md, tree-shaken from production bundle (verified by build-time grep guard). OrbitReel renders FpsBadge under the DEV literal so the dead branch is eliminated in prod.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Lighthouse v1.0.0 baseline + threshold assertion script (PERF-01/03/04 evidence)</name>
  <read_first>
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-RESEARCH.md Pattern 4 + Pitfall 5 (preview NOT dev)
    - .planning/REQUIREMENTS.md (PERF-01 ≥ 90, PERF-03 LCP ≤ 2.5s, PERF-04 CLS ≤ 0.1)
    - package.json (current scripts; add lighthouse:mobile)
    - vite.config.ts (preview port — Vite default 4173)
  </read_first>
  <files>
scripts/lighthouse-baseline.sh,
scripts/assert-lighthouse-thresholds.ts,
package.json,
docs/lighthouse/v1.0.0-baseline.json,
docs/lighthouse/v1.0.0-baseline.html
  </files>
  <action>
1. Create `scripts/lighthouse-baseline.sh` (executable: `chmod +x` after write). Source from RESEARCH.md Pattern 4 — start `bun run preview --host --port 4173` in background, wait for the URL to respond, run `bunx lighthouse` with `--form-factor=mobile --screenEmulation.mobile --throttling.cpuSlowdownMultiplier=4 --output=json --output=html`, symlink the timestamped output to stable paths `docs/lighthouse/v1.0.0-baseline.{json,html}`. Trap on EXIT to kill the preview server PID.

2. Create `scripts/assert-lighthouse-thresholds.ts`:
   ```ts
   import { readFileSync } from 'fs';
   const report = JSON.parse(readFileSync('docs/lighthouse/v1.0.0-baseline.json', 'utf-8'));
   const perf = report.categories.performance.score * 100;
   const lcp = report.audits['largest-contentful-paint'].numericValue;
   const cls = report.audits['cumulative-layout-shift'].numericValue;
   const fails: string[] = [];
   if (perf < 90) fails.push(`perf=${perf} < 90`);
   if (lcp > 2500) fails.push(`LCP=${lcp}ms > 2500ms`);
   if (cls > 0.1) fails.push(`CLS=${cls} > 0.1`);
   if (fails.length) { console.error('Lighthouse thresholds FAIL:\n  ' + fails.join('\n  ')); process.exit(1); }
   console.log(`Lighthouse OK: perf=${perf} LCP=${lcp}ms CLS=${cls}`);
   ```

3. Add package.json scripts:
   - `"lighthouse:baseline": "scripts/lighthouse-baseline.sh"`
   - `"lighthouse:assert": "bun scripts/assert-lighthouse-thresholds.ts"`
   - `"lighthouse:mobile": "bun run lighthouse:baseline && bun run lighthouse:assert"`

4. Run `bun run build && bun run lighthouse:mobile`. Capture the produced JSON + HTML at `docs/lighthouse/v1.0.0-baseline.json` and `docs/lighthouse/v1.0.0-baseline.html`. Both files must exist on disk after the run.

5. **If thresholds miss** (per CONTEXT.md triage #1 instruction): capture the diagnosis (LCP element, blocking resources, what audits failed) in `docs/lighthouse/v1.0.0-baseline-DIAGNOSIS.md`. Then choose ONE: (a) fix in this task if the fix is < 1 commit (e.g., add `loading="lazy"` to non-LCP images, hoist a CSS chunk per memory feedback_lazy_chunk_css.md), OR (b) defer the fix to v1.1 with a TODOS.md entry. Record the decision in SUMMARY.md.

6. The baseline JSON + HTML are checked in as v1.0.0 launch artifacts (`git add docs/lighthouse/`).

Pitfall guard: NEVER run Lighthouse against `bun run dev` — only `bun run preview`. The script enforces this via the hardcoded preview command + port. Per memory feedback_grep_guard_vs_comments.md: the shell script's heredoc/comment text doesn't trip a grep gate, but be careful that the script doesn't contain the string `bun run dev` anywhere.
  </action>
  <verify>
    <automated>test -x scripts/lighthouse-baseline.sh && test -f scripts/assert-lighthouse-thresholds.ts && jq -r '.scripts["lighthouse:mobile"]' package.json | grep -q lighthouse && ! grep -q 'bun run dev' scripts/lighthouse-baseline.sh && bun run lighthouse:mobile && test -f docs/lighthouse/v1.0.0-baseline.json && test -f docs/lighthouse/v1.0.0-baseline.html && jq '.categories.performance.score' docs/lighthouse/v1.0.0-baseline.json | awk '$1+0 >= 0.9 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
- `test -x scripts/lighthouse-baseline.sh` (executable)
- `test -f scripts/assert-lighthouse-thresholds.ts`
- `grep -q "bun run preview" scripts/lighthouse-baseline.sh` (preview server, NOT dev)
- `! grep -q "bun run dev" scripts/lighthouse-baseline.sh` (anti-pitfall)
- `jq -r '.scripts["lighthouse:baseline"]' package.json` returns the script path
- `jq -r '.scripts["lighthouse:assert"]' package.json` matches
- `jq -r '.scripts["lighthouse:mobile"]' package.json` chains baseline + assert
- After `bun run build && bun run lighthouse:mobile`:
  - `test -f docs/lighthouse/v1.0.0-baseline.json` succeeds
  - `test -f docs/lighthouse/v1.0.0-baseline.html` succeeds
  - `jq '.categories.performance.score' docs/lighthouse/v1.0.0-baseline.json` outputs ≥ `0.9` (perf ≥ 90), OR a DIAGNOSIS.md file is captured with explicit "fix vs defer" verdict
- The threshold script either exits 0 (all thresholds met) OR a diagnosis + v1.1 deferral is recorded in `.planning/TODOS.md` and `docs/lighthouse/v1.0.0-baseline-DIAGNOSIS.md` with executor's verdict
  </acceptance_criteria>
  <done>
v1.0.0 Lighthouse mobile baseline produced as JSON + HTML, threshold script asserts perf ≥ 90 / LCP ≤ 2.5s / CLS ≤ 0.1, package.json wired with three scripts. If thresholds miss, diagnosis captured with explicit fix-or-defer verdict.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-only FPS hook → prod bundle | useFrameRate + FpsBadge must NOT reach dist/assets/*.js |
| Auth0 ID-token claims → handle suggestion | nickname/email/given_name are untrusted user input; sanitize chain strips to [a-z0-9-] only (XSS-impossible character set) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | I (Information Disclosure) | FPS badge / useFrameRate leaking telemetry/timing to prod users | mitigate | `import.meta.env.DEV` literal Vite-replaces to `false` at build → Rollup eliminates the branch. Conditional render `{import.meta.env.DEV && <FpsBadge />}` ensures the badge import is dead-coded too. Build-time grep guard `! grep -lE 'useFrameRate\|FpsBadge' dist/assets/*.js` is a tested gate (Task 3). |
| T-11-06 | T (Tampering) | Auth0 nickname/email containing script chars (XSS via handle suggestion) | mitigate | suggestHandle sanitize chain strips to `[a-z0-9-]` only; impossible to express `<script>` or any HTML control character. Final filter through `validateHandle()` (single source of truth at server/handles/validate.ts) blocks reserved words + enforces `{3,20}` length. Task 2 tests this with non-ASCII + reserved-word inputs. |
| T-11-07 | S (Spoofing) / supply-chain | Lighthouse CLI downloads/runs Chromium with arbitrary flags | mitigate | Pinned to lighthouse@13.4.0 (CONTEXT.md D-LOCK). Chrome flags hardcoded in scripts/lighthouse-baseline.sh: `--headless=new --no-sandbox` only. No user-supplied URL inputs — URL is hardcoded to `http://localhost:4173/`. |
| T-11-08 | I (Information Disclosure) | Lighthouse JSON containing cookies / env / session | accept | Runs unauthenticated against `/` (public reel); no cookie state in scope. JSON contains performance metrics only. |

Note: axe-core dev-dep supply chain guarded in 11-01 threat model. Photo modal focus-trap guarded in 11-01.
</threat_model>

<verification>
- All new vitest tests pass under jsdom.
- `bun run build && ! grep -lE 'useFrameRate|FpsBadge' dist/assets/*.js` (tree-shake gate).
- `bun run lighthouse:mobile` exits 0 (or DIAGNOSIS.md captured + TODOS.md entry recorded).
- `git diff src/auth/suggestHandle.ts` is empty (algorithm untouched per D-LOCK).
- REQUIREMENTS.md A11Y-08 reflects shipped semantics.
</verification>

<success_criteria>
- A11Y-02 closed (keyboard add-city test passes, no user.click in the test).
- A11Y-08 closed (REQUIREMENTS.md edited, Enter handler added, keyboard tests pass).
- F8 covered by tests (suggestHandle + HandlePickerGate integration).
- FPS hook + badge ship behind DEV+?fps=1 gate, tree-shaken from prod bundle (build-time grep verified).
- v1.0.0 Lighthouse mobile baseline JSON+HTML on disk; thresholds asserted or diagnosed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-02-SUMMARY.md` covering:
- REQUIREMENTS.md A11Y-08 edit before/after
- Keyboard handlers added (Enter dispatch)
- F8 test results (any algorithm gaps found — flagged, not fixed)
- FPS hook tree-shake guard output (grep result)
- Lighthouse baseline scores (perf / LCP / CLS) and fix-or-defer verdict if thresholds missed
- Manual UAT note: real-iPhone FPS readout via ?fps=1 query string (median + p95 over 8s orbit window; record on completion)
</output>
