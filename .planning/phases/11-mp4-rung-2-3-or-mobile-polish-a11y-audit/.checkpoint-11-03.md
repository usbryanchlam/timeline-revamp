# 11-03 Checkpoint — human-verify gate

**Plan:** 11-03 (visual-review matrix + MP4 cut documentation)
**Gate type:** `checkpoint:human-verify` (Task 2)
**Pause commit:** `f1f3e4c` (Task 1 complete)
**Next task on resume:** Task 3 — MP4 cut documentation + .planning/TODOS.md + TODOS.md v2 entries + 11-SUMMARY.md phase rollup

---

## What was built (Task 1, committed `f1f3e4c`)

11 PNG screenshots produced via Playwright iPhone 13 WebKit emulation against `bun run preview` (port 4173):

- `docs/visual-review/v1.0.0/public-reel-{dark,light}.png`
- `docs/visual-review/v1.0.0/u-bryan-{dark,light}.png`
- `docs/visual-review/v1.0.0/app-reel-{dark,light}.png`
- `docs/visual-review/v1.0.0/app-trips-{dark,light}.png`
- `docs/visual-review/v1.0.0/app-me-{dark,light}.png`
- `docs/visual-review/v1.0.0/u-BRYAN-mixedcase-dark.png` (Phase 7 deferred mobile UAT item #3 re-verification)

`docs/visual-review/v1.0.0/INDEX.md` has the 5×2 matrix skeleton with all cells marked `(pending)` and the four limitations recorded:

1. Playwright WebKit on macOS ≠ iOS Safari (different graphics stacks) — iOS globe projection rendering carries to v1.1.
2. No backend running during `vite preview` → `/u/bryan` (and `/u/BRYAN`) shows the SPA NotFound state.
3. No Auth0 session seeded → `/app/reel`, `/app/trips`, `/app/me` show the logged-out splash / app shell, not authenticated UI.
4. DESIGN.md:72 lock — public reel always dark. The `light` variant for `/` and `/u/:handle` is captured to surface accidental drift.

Photo modals (`PhotoDetailSheet` + `PhotoViewer`) were converted to native `<dialog>` in 11-01 Task 3 — visual layout may have shifted; the matrix surfaces any regression.

---

## Verdicts the user needs to write

Open each PNG in `docs/visual-review/v1.0.0/` and inspect against `DESIGN.md` (single amber accent, public reel always dark, focus rings amber, no surprise non-amber colors).

For each route × theme cell in INDEX.md, flip the text from `(pending)` to one of:

- `ok` — meets DESIGN.md, no regressions
- `issue: <one-line description>` — note the specific drift (centering, contrast, missing element, color drift)

For every `issue:`, decide:

- **Fix in 11-03** (small surgical edit, < 30min) — note in INDEX.md + record in 11-SUMMARY.md (Task 3 will incorporate)
- **Defer to v1.1** — add an entry to `.planning/TODOS.md` AND record the deferral in 11-SUMMARY.md

Also flag any photo-modal layout regression from the `<dialog>` conversion (11-01 Task 3 — `PhotoDetailSheet` bottom-sheet on mobile vs. centered on md+).

`/app/*` screenshots showing the Auth0 redirect / logged-out splash is acceptable per Task 1's recorded limitation — the verdict can be `ok (logged-out splash captured per Task 1 limitation)` or you may flag if it prevents a real verdict.

---

## Resume signal

Reply with either:

- `approved` — all verdicts written into INDEX.md; no further fix needed in this plan. Task 3 (SUMMARY rollup) proceeds.
- Describe issues + your fix-or-defer decisions per cell. Task 3 will incorporate them into the SUMMARY + the `.planning/TODOS.md` v1.1 backlog.

---

## What remains after resume (Task 3)

1. Append to `TODOS.md` (root v2 backlog) with the MP4-04 + MP4-05 cut entries (verbatim strings from the plan, grep-targeted by the verify gate).
2. Append to `.planning/TODOS.md` (Phase 11 — Deferred to v1.1) with the MP4 cut block + iOS globe projection carry + any visual-review deferrals the user chose.
3. Create `.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md` — phase rollup pointing at 11-01-SUMMARY.md + 11-02-SUMMARY.md, recording:
   - Branch D selection (MP4 cut)
   - A11Y-01..08 closure rollup table
   - Lighthouse v1.0.0 baseline scores + verdict
   - Phase 7 deferred mobile UAT items disposition
   - FPS UAT note template (real-iPhone capture)
   - Visual-review matrix reference + deferred items
   - F8 closure note + UAT path
   - Threat model compliance summary
   - Test count delta
   - Carry-forwards to Phase 12
4. Update `.planning/REQUIREMENTS.md` traceability (MP4-04..06 + A11Y-01..08 → Done).
5. Skip ROADMAP.md / STATE.md per executor prompt (orchestrator owns these in Phase 11 close).

The `.planning/STATE.md` Deferred Items + ROADMAP plan-progress updates are owned by the orchestrator post-checkpoint per this executor's prompt; the executor will NOT touch those.
