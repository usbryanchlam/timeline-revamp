---
phase: 02-reel-polish
plan: 05
type: refactor
wave: 1
depends_on: []
files_modified:
  - src/reel/Reel.tsx
  - src/reel/StateBadge.tsx
autonomous: true
requirements:
  - PERF-01
user_setup: []
must_haves:
  truths:
    - "StateBadge does not render in production builds"
    - "StateBadge still renders in dev builds for iPhone gesture verification"
    - "No reference to a 'StateBadge dev affordance shipping in production' remains in CONCERNS.md after this lands"
  artifacts:
    - "src/reel/StateBadge.tsx returns null when import.meta.env.PROD is true"
  key_links:
    - "Reel.tsx still imports StateBadge but the production no-op render is enforced inside the component itself, so future callers don't have to remember the gate"
---

<objective>
Hide the dev-only `StateBadge` from production builds. It exists for the W1 acceptance check (you watched it on the iPhone to verify gesture transitions); it has no place on the public reel.

Purpose: Cleanup item flagged in `.planning/codebase/CONCERNS.md`. Small surface — a tiny amber pill in the top-left — but visible to recruiters and inconsistent with the cinematic-editorial aesthetic. Plus, it ships unnecessary text in the production HTML.

Output: StateBadge gates itself behind import.meta.env.DEV; CONCERNS.md updated to note the issue is closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md

@src/reel/StateBadge.tsx
@src/reel/Reel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-gate StateBadge inside the component</name>
  <files>src/reel/StateBadge.tsx</files>
  <read_first>src/reel/StateBadge.tsx</read_first>
  <action>At the top of the StateBadge function body, add an early `return null` guard that triggers in production: `if (import.meta.env.PROD) return null;`. Place it as the very first line of the component body, before the JSX return. This way, callers (Reel.tsx) don't need to know about the gate — the component is universally safe to render.

  Why self-gate vs gate at call site: keeps Reel.tsx cleaner (no env check littered there), and means if any future dev surface (route page, debug panel) wants to render StateBadge, they get the gating for free.

  Why `import.meta.env.PROD` not `!import.meta.env.DEV`: Vite docs recommend PROD for "is this a production build" checks; DEV is true in dev server. Both technically work; PROD is the canonical name.</action>
  <verify>bun run build && grep -L "STATE" dist/assets/index-*.js && grep -L "ReelStateName" dist/assets/index-*.js</verify>
  <acceptance_criteria>
    - "src/reel/StateBadge.tsx contains 'import.meta.env.PROD'"
    - "After bun run build, the production JS bundle does not contain the literal text 'IDLE' or 'SCRUBBING' from a StateBadge render path (Vite + tree-shaking should DCE the JSX entirely when PROD branch returns null and is statically known)"
    - "bun run typecheck still passes"
  </acceptance_criteria>
  <done>Production builds emit no StateBadge DOM; dev builds still show it.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm Reel.tsx still imports and renders it for dev visibility</name>
  <files>src/reel/Reel.tsx</files>
  <read_first>src/reel/Reel.tsx</read_first>
  <action>Verify Reel.tsx still does `<StateBadge state={state.name} />` — no changes required. The component now self-gates. If Reel.tsx had any conditional wrapping around StateBadge, remove it (we don't want double-gating; the component owns its visibility).</action>
  <verify>grep -n "StateBadge" src/reel/Reel.tsx</verify>
  <acceptance_criteria>
    - "src/reel/Reel.tsx contains '<StateBadge'"
    - "src/reel/Reel.tsx does NOT contain 'import.meta.env' near the StateBadge render"
  </acceptance_criteria>
  <done>Reel.tsx is unchanged or simplified; StateBadge gating lives in one place.</done>
</task>

<task type="auto">
  <name>Task 3: Update CONCERNS.md to mark issue closed</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>.planning/codebase/CONCERNS.md</read_first>
  <action>Find the section "**`StateBadge` is ungated:**" under "## Tech Debt". Replace its body with a one-line note: "Closed in 02-05 — StateBadge now self-gates via `import.meta.env.PROD`." Optionally move the entire item out of "Tech Debt" into a new "## Resolved" section at the bottom of the file with a date marker. Either pattern works; pick the one that keeps CONCERNS.md cleanest.</action>
  <verify>grep -A 1 "StateBadge" .planning/codebase/CONCERNS.md</verify>
  <acceptance_criteria>
    - ".planning/codebase/CONCERNS.md mentions StateBadge"
    - ".planning/codebase/CONCERNS.md notes the issue is closed in 02-05"
  </acceptance_criteria>
  <done>CONCERNS.md no longer flags this as active tech debt.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run typecheck passes
- [ ] bun run build passes
- [ ] bun run dev — StateBadge still visible (top-left amber pill showing current ReelStateName)
- [ ] bun run preview (production build) — StateBadge NOT visible anywhere on the reel
- [ ] grep -c "StateBadge" dist/assets/index-*.js returns 0 (component tree-shaken from prod build)
</verification>

<success_criteria>

- All tasks completed
- Public visitors no longer see the dev affordance
- Dev workflow unchanged (badge still appears for in-session verification)
- CONCERNS.md reflects closed status
</success_criteria>
