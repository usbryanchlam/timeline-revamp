---
status: partial
phase: 08.1-infra-terraform
source: [08.1-VERIFICATION.md]
started: 2026-05-27T05:37:54Z
updated: 2026-05-27T05:37:54Z
---

## Current Test

[awaiting operator handoff to Phase 8 Wave 3 resume]

## Tests

### 1. First `terraform apply` against live OCI tenancy
expected: VCN + VM + Reserved Public IP + photos bucket + IAM + IDCS users + propagation trust all provision; `terraform output public_ip` returns a routable IP
result: [pending]
context: Requires live OCI credentials + tenancy access; may hit Ampere A1 "Out of host capacity" (OCI-side, 5-20 retries documented in DEPLOY.md Troubleshooting per CONTEXT D-12 + RESEARCH Pitfall 6)

### 2. First GHA workflow run on a real PR
expected: `plan` job posts terraform-plan comment on the PR using `gha_pr_reader` OIDC identity; `apply` job gated by `environment: production` waits for usbryanchlam approval
result: [pending]
context: Validates the OIDC token-exchange roundtrip end-to-end (GHA JWT → OCI `/oauth2/v1/token` → UPST → `terraform apply`). Cannot be simulated locally — requires the live Identity Propagation Trust and the GitHub Environment reviewer gate.

### 3. Weekly drift-detection cron fires
expected: First Monday 12:00 UTC after deploy, the `terraform` workflow runs on schedule; if `terraform plan` returns exit 2 the `actions/github-script` step opens a labelled drift Issue (or appends to the existing one)
result: [pending]
context: Time-based — requires waiting for a real cron trigger. No reliable way to simulate without manually editing the workflow.

### 4. cloud-init renders + applies on first-boot of the live VM
expected: VM reaches "Cloud-init complete." final_message; `ubuntu` user in `docker` group; `iptables -L INPUT -n --line-numbers` shows ACCEPT for 80/443 ABOVE any REJECT rule; `/opt/timeline-revamp` cloned + owned by ubuntu
result: [pending]
context: cloud-init schema validation deferred (cloud-init not installed on macOS workstation); only validates against a real Ubuntu kernel on the live VM.

### 5. CORS round-trip via aws s3api after first apply
expected: `aws s3api get-bucket-cors --endpoint-url <s3-compat> --bucket timeline-photos` returns the rules we set (3 origins, 3 methods including PUT, ETag/Content-Length/x-amz-version-id exposed, 3600 max-age); `null_resource.photos_cors` provisioner exit 0
result: [pending]
context: D-15 (corrected) goes through `null_resource` + `aws s3api` because OCI provider lacks native cors_rules. First apply validates the path actually works end-to-end against the live S3-compat endpoint with `AWS_REQUEST_CHECKSUM_CALCULATION=when_required`.

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
