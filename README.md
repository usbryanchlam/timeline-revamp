# timeline-revamp

A reimagined travel-tracker, built from scratch. The original project lives at
`../timeline/` and is retained as reference only — this is not a migration.

## Status

Scaffolding. Product scope and design system to be defined via the gstack
pipeline (`/office-hours` → `/design-consultation` → `/autoplan` → build).

## Stack

TBD — decided during planning.
