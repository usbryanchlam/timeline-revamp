---
phase: 02-reel-polish
plan: 02
type: feature
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/reel/Reel.tsx
  - src/reel/MapCanvas.tsx
  - src/reel/MapPoster.tsx
  - public/poster-tokyo.jpg
  - index.html
  - vite.config.ts
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-05
user_setup:
  - "Capture a 1280x720 JPEG screenshot of the Tokyo first-chapter map view at production zoom (after 02-01 lands), save to public/poster-tokyo.jpg. Optional helper: bun run dev, full-screen the iPhone-portrait viewport at the Tokyo chapter, screenshot the canvas region. If you skip this, the LCP image will be a placeholder gradient instead of a real map frame."
must_haves:
  truths:
    - "Initial JS bundle (excluding MapLibre) is ≤ 250 KB gzipped per PERF-02"
    - "MapLibre is loaded after first paint, not blocking it"
    - "LCP element is a static <img> poster, not the WebGL canvas"
    - "First contentful paint shows the poster within ~500ms on a fast connection"
  artifacts:
    - "src/reel/MapPoster.tsx renders the LCP <img> with proper sizing"
    - "src/reel/Reel.tsx uses React.lazy + Suspense to defer MapCanvas import"
    - "public/poster-tokyo.jpg exists and is preloaded via <link rel=\"preload\">"
  key_links:
    - "MapPoster covers the same screen area MapCanvas will replace, no layout shift on swap"
    - "vite.config.ts manualChunks (or rollupOptions) splits maplibre-gl into its own chunk"
---

<objective>
Code-split MapLibre out of the main JS chunk and use a pre-rendered poster image as the LCP element. The map canvas mounts asynchronously after first paint.

Purpose: Deliver PERF-02 (≤250 KB gzipped initial JS excluding MapLibre), PERF-03 (LCP poster path), PERF-05 (WebGL init after LCP). This is the load-bearing perf work for the Lighthouse 90 budget gate at end of phase.

Output: MapPoster component as LCP element, lazy-loaded MapCanvas, manualChunks config splitting maplibre-gl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md

@src/reel/Reel.tsx
@src/reel/MapCanvas.tsx
@vite.config.ts
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MapPoster component for the LCP path</name>
  <files>src/reel/MapPoster.tsx</files>
  <read_first>src/reel/Reel.tsx</read_first>
  <action>Create `src/reel/MapPoster.tsx`. It renders an absolutely-positioned `<img>` covering the full reel surface (same `inset-0` positioning the map canvas uses), with `src="/poster-tokyo.jpg"`, `alt=""` (decorative), `width={1280}`, `height={720}`, `style={{ objectFit: 'cover' }}`. Add `fetchPriority="high"` and `decoding="async"`. Include a fallback solid-color background (`bg-bg-map`) so reduced-bandwidth users see something during the JPEG fetch. Component is named `MapPoster`, exported.</action>
  <verify>bun run typecheck — confirm no TS errors. The image path is allowed even if the file doesn't yet exist.</verify>
  <acceptance_criteria>
    - "src/reel/MapPoster.tsx exports a function component named MapPoster"
    - "src/reel/MapPoster.tsx contains 'fetchPriority' and 'decoding'"
    - "src/reel/MapPoster.tsx references '/poster-tokyo.jpg'"
  </acceptance_criteria>
  <done>MapPoster.tsx written; bun run typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Lazy-import MapCanvas in Reel.tsx</name>
  <files>src/reel/Reel.tsx</files>
  <read_first>src/reel/Reel.tsx</read_first>
  <action>Replace the static `import { MapCanvas } from './MapCanvas'` with `const MapCanvas = lazy(() => import('./MapCanvas').then(m => ({ default: m.MapCanvas })))`. Wrap the `<MapCanvas .../>` JSX in `<Suspense fallback={<MapPoster />}>`. Import `MapPoster` and `Suspense` + `lazy` from React. The poster shows immediately on first render; MapCanvas swaps in once the chunk arrives. This is the LCP-first pattern.</action>
  <verify>bun run typecheck && bun run dev — observe poster image flash before map mounts (use Network throttling in DevTools to make it visible).</verify>
  <acceptance_criteria>
    - "src/reel/Reel.tsx imports 'lazy' and 'Suspense' from 'react'"
    - "src/reel/Reel.tsx uses 'lazy(() =>' with a dynamic import of './MapCanvas'"
    - "src/reel/Reel.tsx renders <Suspense fallback={<MapPoster />}>"
    - "bun run typecheck exits 0"
  </acceptance_criteria>
  <done>MapCanvas split into its own chunk, MapPoster fills the gap during load.</done>
</task>

<task type="auto">
  <name>Task 3: Preload the poster in index.html</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>Add `<link rel="preload" as="image" href="/poster-tokyo.jpg" fetchpriority="high">` inside the `<head>` block, BEFORE the Google Fonts stylesheet link (the poster is more important for LCP than fonts). Keep the existing preconnects.</action>
  <verify>cat index.html | grep -A 1 "poster-tokyo"</verify>
  <acceptance_criteria>
    - "index.html contains '<link rel=\"preload\"' and 'poster-tokyo.jpg'"
    - "The preload link appears before the Google Fonts stylesheet link in the file"
  </acceptance_criteria>
  <done>Browser preloads the poster aggressively on document parse.</done>
</task>

<task type="auto">
  <name>Task 4: Configure manualChunks to split maplibre-gl</name>
  <files>vite.config.ts</files>
  <read_first>vite.config.ts</read_first>
  <action>Add a `build.rollupOptions.output.manualChunks` config that puts `maplibre-gl` in its own chunk: `manualChunks: { 'maplibre': ['maplibre-gl'] }`. Keep the existing `plugins`, `resolve`, `server` config untouched. This is belt-and-braces with the lazy-import: lazy-import lets the chunk load on-demand; manualChunks ensures it's a separate file users can cache across deploys.</action>
  <verify>bun run build — observe `dist/assets/maplibre-*.js` chunk in the output, separate from `dist/assets/index-*.js`. The index chunk's gzip size should drop dramatically.</verify>
  <acceptance_criteria>
    - "vite.config.ts contains 'manualChunks' and 'maplibre-gl'"
    - "After bun run build, dist/assets/ contains a file matching 'maplibre*.js'"
    - "After bun run build, the main index-*.js gzip size is < 100 KB"
  </acceptance_criteria>
  <done>maplibre-gl is its own chunk; main bundle size targets PERF-02.</done>
</task>

<task type="auto">
  <name>Task 5: Verify and place the poster JPEG (if user_setup completed)</name>
  <files>public/poster-tokyo.jpg</files>
  <read_first>public/</read_first>
  <action>If `public/poster-tokyo.jpg` exists, verify it is approximately 1280x720 and ≤ 200 KB. Use `file public/poster-tokyo.jpg` to check format and `ls -la public/poster-tokyo.jpg` for size. If the file is missing OR too large (>200 KB), STOP and surface the issue: ask the user to re-capture or compress (recommend `cwebp -q 80` or `jpegoptim --max=80`) and re-run. If file is missing entirely, exit this task with a checkpoint asking the user to run user_setup before continuing the plan.</action>
  <verify>file public/poster-tokyo.jpg && ls -la public/poster-tokyo.jpg</verify>
  <acceptance_criteria>
    - "public/poster-tokyo.jpg exists"
    - "File reports as JPEG image data"
    - "File size ≤ 200 KB"
  </acceptance_criteria>
  <done>Poster JPEG present and sized appropriately for LCP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code-splitting + LCP poster pipeline. Dev server should serve a static poster on first paint, then asynchronously hydrate the WebGL canvas.</what-built>
  <how-to-verify>1) Run `bun run build && bun run preview --host`. 2) Open Chrome DevTools → Lighthouse → Mobile profile → run audit. Confirm: LCP element is `<img>`, perf score ≥ 90, "Reduce unused JavaScript" is no longer flagged for the main chunk. 3) On iPhone: open the LAN URL, observe poster appears before map (especially noticeable on slower wifi). Network panel should show `maplibre-*.js` loading after `index-*.js`.</how-to-verify>
  <resume-signal>Type "lighthouse passes" with the score, or paste the failure breakdown.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run typecheck passes
- [ ] bun run build emits separate maplibre-*.js chunk
- [ ] Main chunk gzip ≤ 250 KB (run `ls -la dist/assets/index-*.js` and check the gzipped equivalent reported in vite output)
- [ ] Lighthouse mobile perf ≥ 90 on `bun run preview` (manual)
- [ ] LCP element is the <img>, not the canvas (Lighthouse panel confirms)
- [ ] No layout shift when MapCanvas mounts (CLS in Lighthouse ≤ 0.1)
</verification>

<success_criteria>

- All tasks completed and checkpoints approved
- PERF-02, PERF-03, PERF-05 satisfied (verified via Lighthouse)
- MapLibre is now a deferred chunk users only pay for after first paint
- Reel still functions identically once MapCanvas hydrates
</success_criteria>
