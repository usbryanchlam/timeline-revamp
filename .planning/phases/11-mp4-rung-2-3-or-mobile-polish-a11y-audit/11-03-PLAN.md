---
phase: 11-mp4-rung-2-3-or-mobile-polish-a11y-audit
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - e2e/visual-review.spec.ts
  - docs/visual-review/v1.0.0/INDEX.md
  - docs/visual-review/v1.0.0/public-reel-dark.png
  - docs/visual-review/v1.0.0/public-reel-light.png
  - docs/visual-review/v1.0.0/u-bryan-dark.png
  - docs/visual-review/v1.0.0/u-bryan-light.png
  - docs/visual-review/v1.0.0/app-reel-dark.png
  - docs/visual-review/v1.0.0/app-reel-light.png
  - docs/visual-review/v1.0.0/app-trips-dark.png
  - docs/visual-review/v1.0.0/app-trips-light.png
  - docs/visual-review/v1.0.0/app-me-dark.png
  - docs/visual-review/v1.0.0/app-me-light.png
  - TODOS.md
  - .planning/TODOS.md
  - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md
autonomous: false
requirements: [MP4-04, MP4-05, MP4-06]
tags: [visual-review, mp4-cut, summary, playwright, screenshots]

must_haves:
  truths:
    - "Playwright iPhone-13 emulation matrix produces 10 screenshots: 5 routes × {dark, light} themes — all saved to docs/visual-review/v1.0.0/"
    - "docs/visual-review/v1.0.0/INDEX.md contains a 5×2 verdict table with one of {ok, issue: <text>} per cell"
    - "Every 'issue:' verdict is either fixed in-phase OR converted to a v1.1 entry in .planning/TODOS.md before phase close"
    - "MP4-04 (rung 2 client MediaRecorder) is documented as cut from v1 in 11-SUMMARY.md narrative and TODOS.md v2 backlog"
    - "MP4-05 (rung 3 GIF fallback) is documented as cut from v1 in 11-SUMMARY.md narrative and TODOS.md v2 backlog"
    - "MP4-06 (cut path) is satisfied by 11-SUMMARY.md narrative recording the Branch D selection — closing the cut path per D-LOCK"
    - "11-SUMMARY.md is the phase-level rollup pointing at 11-01-SUMMARY.md + 11-02-SUMMARY.md and recording deferred Phase 7 mobile UAT items (iOS globe projection → v1.1; mixed-case URLs → already addressed)"
    - "User checkpoint confirms visual-review verdicts and the MP4 cut narrative before phase closes"
  artifacts:
    - path: "e2e/visual-review.spec.ts"
      provides: "Playwright iPhone 13 + colorScheme emulation matrix; 10 PNG screenshots into docs/visual-review/v1.0.0/"
      contains: "colorScheme"
    - path: "docs/visual-review/v1.0.0/INDEX.md"
      provides: "Route × theme verdict table"
      contains: "verdict"
    - path: "TODOS.md"
      provides: "Root v2 backlog entry for MP4-04 + MP4-05"
      contains: "MP4-04"
    - path: ".planning/TODOS.md"
      provides: "Planning v2 backlog entry for MP4 cut + iOS globe projection + any visual-review issues deferred"
      contains: "MP4 cut from v1"
    - path: ".planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md"
      provides: "Phase rollup: A11Y status, MP4 cut narrative, visual-review verdicts, FPS UAT note, Lighthouse baseline scores"
      contains: "Branch D"
  key_links:
    - from: "e2e/visual-review.spec.ts"
      to: "docs/visual-review/v1.0.0/{slug}-{theme}.png"
      via: "page.screenshot({ path })"
      pattern: "page\\.screenshot"
    - from: "11-SUMMARY.md"
      to: "TODOS.md + .planning/TODOS.md"
      via: "Cut decision references the v2 backlog entries"
      pattern: "MP4-0[456]"
    - from: "11-SUMMARY.md"
      to: "11-01-SUMMARY.md + 11-02-SUMMARY.md"
      via: "phase rollup section references prior plan summaries"
      pattern: "11-0[12]-SUMMARY"
---

<objective>
Produce the v1.0.0 iPhone visual-review matrix (Playwright iPhone 13 emulation × {dark, light}), close MP4-04/05/06 via documentation per Branch D, and write the phase-level 11-SUMMARY.md rollup.

Purpose: This is the closing plan. The visual-review matrix is the v1.0.0 launch baseline (carries forward to v1.1 for regression detection). MP4 cut requires a narrative in SUMMARY + v2 backlog entry — per D-LOCK, that IS the satisfaction of MP4-06.

Output:
- 10 PNG screenshots (5 routes × 2 themes) in docs/visual-review/v1.0.0/
- INDEX.md with per-cell verdict
- TODOS.md + .planning/TODOS.md updated with MP4-04/05 v2 entries + any visual-review issues deferred to v1.1
- 11-SUMMARY.md rollup with MP4 cut narrative + Phase 7 mobile UAT deferral disposition + FPS UAT note pointer
- Human checkpoint to confirm verdicts + cut narrative before phase closes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-RESEARCH.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-VALIDATION.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-01-PLAN.md
@.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-02-PLAN.md
@DESIGN.md
@playwright.config.ts
@TODOS.md
@.planning/TODOS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Playwright iPhone 13 visual-review matrix — 5 routes × 2 themes = 10 PNGs</name>
  <read_first>
    - playwright.config.ts (created in 11-01 — iPhone 13 device + preview webServer)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-RESEARCH.md "Visual-review screenshot matrix" code example (line ~600)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md (triage #5 spec — 5 routes, 2 themes, 10 screenshots, INDEX.md verdict)
    - DESIGN.md (public reel always dark — line 72; expect dark to dominate / public reel light is still dark theme — note in INDEX.md)
  </read_first>
  <files>
e2e/visual-review.spec.ts,
docs/visual-review/v1.0.0/INDEX.md,
docs/visual-review/v1.0.0/public-reel-dark.png,
docs/visual-review/v1.0.0/public-reel-light.png,
docs/visual-review/v1.0.0/u-bryan-dark.png,
docs/visual-review/v1.0.0/u-bryan-light.png,
docs/visual-review/v1.0.0/app-reel-dark.png,
docs/visual-review/v1.0.0/app-reel-light.png,
docs/visual-review/v1.0.0/app-trips-dark.png,
docs/visual-review/v1.0.0/app-trips-light.png,
docs/visual-review/v1.0.0/app-me-dark.png,
docs/visual-review/v1.0.0/app-me-light.png
  </files>
  <action>
1. Create `e2e/visual-review.spec.ts`:
   ```ts
   import { test, devices } from '@playwright/test';

   test.use({ ...devices['iPhone 13'] });

   const ROUTES = [
     { path: '/', slug: 'public-reel' },
     { path: '/u/bryan', slug: 'u-bryan' },
     { path: '/app/reel', slug: 'app-reel' },
     { path: '/app/trips', slug: 'app-trips' },
     { path: '/app/me', slug: 'app-me' },
   ] as const;
   const THEMES = ['dark', 'light'] as const;

   for (const { path, slug } of ROUTES) {
     for (const theme of THEMES) {
       test(`visual ${slug} ${theme}`, async ({ page }) => {
         await page.emulateMedia({ colorScheme: theme });
         await page.goto(path);
         await page.waitForLoadState('networkidle');
         await page.screenshot({
           path: `docs/visual-review/v1.0.0/${slug}-${theme}.png`,
           fullPage: false,
         });
       });
     }
   }
   ```

2. Build + run: `bun run build && bun run e2e --grep "visual" --project=webkit-iphone`. Expect 10 PNGs.

3. Authentication note: `/app/reel`, `/app/trips`, `/app/me` are behind `<RequireAuth>`. The Playwright run will land on the Auth0 universal-login page (an external host) if auth isn't stubbed. Two acceptable paths:
   - (a) Use Playwright auth state pre-seeded once locally via `bunx playwright codegen` against a test account, save to `e2e/.auth/user.json`, load with `test.use({ storageState })`.
   - (b) Accept that the `/app/*` screenshots will show the Auth0 redirect or a logged-out splash; record the limitation in INDEX.md and capture authenticated screenshots manually on real iPhone.
   - Executor picks (a) if local Auth0 test account is configured; otherwise (b) and a manual capture is added to the SUMMARY UAT note.

4. `/u/bryan` resolution: if no user with handle `bryan` exists in the local DB, either seed one OR substitute a known-public handle (note in INDEX.md). Local DB seeding is at executor's discretion.

5. After screenshots are produced, write `docs/visual-review/v1.0.0/INDEX.md` with a 5×2 verdict table. Initially fill all cells with `(pending review)`. The next task includes the human review checkpoint that flips each cell to `ok` or `issue: <text>`.

   ```markdown
   # Visual Review Matrix — v1.0.0
   Captured 2026-MM-DD via Playwright iPhone 13 emulation. Real-device UAT supplementary (see 11-SUMMARY.md UAT note).

   **Limitation:** Playwright WebKit on macOS uses macOS's graphics stack; iOS Safari uses Metal on Apple GPU. Carry "iOS globe projection rendering" as v1.1 TODOS.md item per CONTEXT.md.

   | Route | Dark theme | Light theme |
   |-------|------------|-------------|
   | `/` (public reel) | ![dark](public-reel-dark.png) — (pending) | ![light](public-reel-light.png) — (pending; DESIGN.md:72 lock — public reel always dark) |
   | `/u/bryan` | ![dark](u-bryan-dark.png) — (pending) | ![light](u-bryan-light.png) — (pending; same lock as above) |
   | `/app/reel` | ![dark](app-reel-dark.png) — (pending) | ![light](app-reel-light.png) — (pending) |
   | `/app/trips` | ![dark](app-trips-dark.png) — (pending) | ![light](app-trips-light.png) — (pending) |
   | `/app/me` | ![dark](app-me-dark.png) — (pending) | ![light](app-me-light.png) — (pending) |

   ## Verdicts
   (Filled by human review checkpoint in 11-03 Task 2.)
   ```

6. Phase 7 deferred mobile UAT item #3 (mixed-case URL resolution) re-verification: hit `/u/BRYAN` once and confirm it redirects/resolves to `/u/bryan` per the lowercase normalization shipped in Phase 7-02. Note outcome in INDEX.md (one line). Per CONTEXT.md deferred ideas, this is the only re-verification action required — no plan task otherwise.
  </action>
  <verify>
    <automated>test -f e2e/visual-review.spec.ts && bun run build && bun run e2e --grep "visual" && ls docs/visual-review/v1.0.0/*.png | wc -l | awk '$1>=10 {exit 0} {exit 1}' && test -f docs/visual-review/v1.0.0/INDEX.md</automated>
  </verify>
  <acceptance_criteria>
- `test -f e2e/visual-review.spec.ts`
- `grep -c "page.screenshot" e2e/visual-review.spec.ts` outputs at least `1`
- `grep -c "colorScheme" e2e/visual-review.spec.ts` outputs at least `1`
- `ls docs/visual-review/v1.0.0/*.png | wc -l` outputs at least `10` (5 routes × 2 themes; >10 ok if authenticated variants captured separately)
- All 10 required filenames present: `for f in public-reel-dark public-reel-light u-bryan-dark u-bryan-light app-reel-dark app-reel-light app-trips-dark app-trips-light app-me-dark app-me-light; do test -f "docs/visual-review/v1.0.0/$f.png" || exit 1; done`
- `test -f docs/visual-review/v1.0.0/INDEX.md && grep -c '| Route ' docs/visual-review/v1.0.0/INDEX.md` outputs at least `1` (table header present)
- INDEX.md contains the DESIGN.md:72 note about public reel always dark: `grep -q "always dark" docs/visual-review/v1.0.0/INDEX.md`
- Mixed-case URL re-verification noted: `grep -qE "mixed.case|/u/BRYAN" docs/visual-review/v1.0.0/INDEX.md`
  </acceptance_criteria>
  <done>
10 PNG screenshots produced and committed under docs/visual-review/v1.0.0/. INDEX.md skeleton in place with pending verdicts. Authentication strategy + Phase 7 mixed-case URL re-verification recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual review — verdict per cell + decide fix/defer per issue</name>
  <what-built>
The Playwright matrix produced 10 screenshots:
- docs/visual-review/v1.0.0/public-reel-{dark,light}.png
- docs/visual-review/v1.0.0/u-bryan-{dark,light}.png
- docs/visual-review/v1.0.0/app-reel-{dark,light}.png
- docs/visual-review/v1.0.0/app-trips-{dark,light}.png
- docs/visual-review/v1.0.0/app-me-{dark,light}.png

INDEX.md is in place with pending verdicts. Photo modals (PhotoDetailSheet + PhotoViewer) were converted to native &lt;dialog&gt; in 11-01 Task 3 — visual layout may have shifted; check.
  </what-built>
  <how-to-verify>
1. Open each PNG in `docs/visual-review/v1.0.0/` and inspect against DESIGN.md (single amber accent, public reel always dark, focus rings amber, no surprise non-amber colors).
2. For each route × theme cell, flip the INDEX.md text from `(pending)` to one of:
   - `ok` — meets DESIGN.md, no regressions
   - `issue: <one-line description>` — note the specific drift (centering, contrast, missing element, color drift)
3. For every `issue:`, decide:
   - **Fix in 11-03** (small surgical edit, < 30min) — note in INDEX.md + record in 11-SUMMARY.md
   - **Defer to v1.1** — add an entry to `.planning/TODOS.md` AND record the deferral in 11-SUMMARY.md
4. Note any photo-modal layout regression from the &lt;dialog&gt; conversion (11-01 Task 3) — bottom-sheet on mobile vs. centered on md+ must look right.
5. Confirm `/app/*` screenshots are usable (if auth wasn't stubbed in Task 1, the screenshots may show the Auth0 redirect splash — that is acceptable per Task 1's recorded limitation, but flag if it prevents a real verdict).

Resume by replying with either:
- `approved` (all verdicts written into INDEX.md; no further fix needed in this plan), OR
- describe issues + your fix-or-defer decisions; Task 3 (SUMMARY rollup) will incorporate them.
  </how-to-verify>
  <resume-signal>Type `approved` or describe issues + fix-or-defer decisions per cell</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MP4 cut documentation + .planning/TODOS.md + TODOS.md v2 entries + 11-SUMMARY.md phase rollup</name>
  <read_first>
    - .planning/REQUIREMENTS.md (MP4-04/05/06 exact text + status table line ~165)
    - .planning/ROADMAP.md (Phase 11 Branch D success criteria + Phase 12 dependency line)
    - .planning/STATE.md (Deferred Items table; need to add MP4 cut row)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-CONTEXT.md (D-LOCK Branch D narrative + Phase 7 deferred items disposition)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-01-SUMMARY.md (produced by 11-01)
    - .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-02-SUMMARY.md (produced by 11-02)
    - TODOS.md (repo-root v2 backlog)
    - .planning/TODOS.md (Phase-3-internal backlog)
    - docs/visual-review/v1.0.0/INDEX.md (verdicts from Task 2)
    - docs/lighthouse/v1.0.0-baseline.json (scores produced by 11-02)
  </read_first>
  <files>
TODOS.md,
.planning/TODOS.md,
.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md
  </files>
  <action>
1. Append to `TODOS.md` (root v2 backlog) — under whatever existing "Deferred to v2" heading exists, add (verbatim — these strings are grep-targeted by the verify gate):
   ```
   - MP4-04 (rung 2 fallback): Client `MediaRecorder` + `canvas.captureStream(30)` — cut from v1 per Phase 11 Branch D selection (Phase 10 server-side MP4 stayed on hold; Branches A/B/C not pursued). Revisit in v2.
   - MP4-05 (rung 3 fallback): 10-second looping GIF export — cut from v1 per Phase 11 Branch D selection. Revisit in v2.
   ```

2. Append to `.planning/TODOS.md` (under existing heading or create a "Phase 11 deferred" section):
   ```
   ## Phase 11 — Deferred to v1.1
   - **MP4 cut from v1** (Branch D selection): MP4-04 (rung 2 MediaRecorder) and MP4-05 (rung 3 GIF) cut from v1; MP4-06 satisfied by this documentation. Phase 10 server-side rung 1 remains on hold. Tracking entry for v2.
   - **iOS globe projection rendering** (deferred Phase 7 mobile UAT item #2): Playwright WebKit emulation does not exercise iOS Metal/GPU stack; bug cannot be closed by visual-review matrix. Carries to v1.1.
   - (Add any "issue:" verdicts from visual-review matrix Task 2 that the user chose to defer.)
   - (Add Lighthouse threshold misses from 11-02 Task 4 that the user chose to defer, if any.)
   ```

3. Create `.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md`. This is the phase rollup pointing at 11-01-SUMMARY.md + 11-02-SUMMARY.md. EXACT structure:
   ```markdown
   ---
   phase: 11-mp4-rung-2-3-or-mobile-polish-a11y-audit
   branch: D
   status: complete
   completed: 2026-MM-DD
   ---

   # Phase 11 — SUMMARY (Branch D: MP4 cut from v1; mobile polish + a11y audit)

   ## Branch Selection
   **Branch D selected.** MP4 cut from v1 — Phase 10 server-side rung 1 stayed on hold; Branches A (Phase 10 outcome — not pursued), B (client MediaRecorder MP4-04 — cut), C (GIF MP4-05 — cut) all deferred to v2.

   - **MP4-04**: Cut from v1. See TODOS.md + .planning/TODOS.md.
   - **MP4-05**: Cut from v1. See TODOS.md + .planning/TODOS.md.
   - **MP4-06**: Satisfied by this narrative — the cut path documented per the requirement text ("If all three rungs fail, MP4 is cut from v1 and shipped as v2 feature").

   ## A11Y Status (closes A11Y-01..08)
   See .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-01-SUMMARY.md and 11-02-SUMMARY.md for per-requirement evidence. Rollup:

   | Req | Status | Evidence |
   |-----|--------|----------|
   | A11Y-01 | closed | 0 axe violations on reduced-motion path (3 reels) + Playwright sweep across 5 routes (11-01 Task 2, Task 3) |
   | A11Y-02 | closed | TripsRoute.a11y.test.tsx — keyboard-only add-city, no user.click (11-02 Task 1) |
   | A11Y-03 | closed | role="region" + aria-label on all reduced-motion reels (11-01 Task 2) |
   | A11Y-04 | closed | aria-live gated on state.name IDLE\|PAUSED — fires on arrival pulse (11-01 Task 2) |
   | A11Y-05 | closed | PhotoCycle alt strictly derives from caption with empty-string fallback (11-01 Task 2) |
   | A11Y-06 | closed | PhotoDetailSheet + PhotoViewer converted to native <dialog> with close-watcher anti-modal-trap pattern (11-01 Task 3) |
   | A11Y-07 | closed | Playwright + axe color-contrast rule passes on reduced-motion route sweep (11-01 Task 3) |
   | A11Y-08 | closed | REQUIREMENTS.md edited (drop ←/→ scrub); Enter→OPEN_DETAIL handler added (11-02 Task 1) |

   ## Perf
   Lighthouse v1.0.0 mobile baseline: docs/lighthouse/v1.0.0-baseline.{json,html}. Scores: perf=<from JSON>, LCP=<from JSON>ms, CLS=<from JSON>. (Fill in from actual run output.) If thresholds missed, see DIAGNOSIS file + .planning/TODOS.md deferral.

   ## Phase 7 Deferred Mobile UAT Items — Disposition
   1. **iPhone 60FPS orbit sustain** — closed in Phase 11 (FPS hook + iPhone UAT note below).
   2. **iOS globe projection rendering** — deferred to v1.1 per CONTEXT.md; carries in .planning/TODOS.md.
   3. **Mixed-case URL resolution on deployed stack** — re-verified via visual-review matrix `/u/BRYAN` redirect; closed in v0.2.x patches via Phase 7-02 lowercase normalization.

   ## FPS UAT Note (real iPhone, ?fps=1)
   On iPhone Safari, navigate to `/?fps=1`, trigger the single-city orbit (REEL-08 via /u/<single-city-handle>) or hit a single-city reel directly. Capture median + p95 over the 8s window from the amber FpsBadge at top-left:
   - Median FPS: __ (target ≥ 55)
   - p95-low FPS: __ (target ≥ 50)
   - Verdict: __ (pass / fail; if fail, bottleneck note + carry to v1.1)
   (Filled by real-device run; updated post-checkpoint.)

   ## Visual Review Matrix
   docs/visual-review/v1.0.0/INDEX.md. Per-cell verdicts recorded (Task 2 checkpoint). Issues deferred to v1.1 listed in .planning/TODOS.md.

   ## F8 Closure (Phase 8 finding)
   HandlePickerModal pre-fill from Auth0 nickname/email-local/given_name — algorithm shipped pre-Phase-11; tests + integration coverage added in 11-02 Task 2. UAT note: __ (filled after real-Auth0 login confirms suggestion appears).

   ## Threat Model Compliance
   - axe-core + Playwright + Lighthouse dev-only (verified at build via tree-shake grep + devDependencies separation).
   - FPS hook tree-shaken from prod bundle (build-time grep guard, 11-02 Task 3).
   - suggestHandle sanitize chain strips to [a-z0-9-] only — XSS-impossible character set.

   ## Test Count Delta
   Approx +X tests this phase (a11y, focus-trap, keyboard, suggestHandle, useFrameRate). Exact count from `bun test --reporter=verbose | tail`.

   ## Carry-forwards to Phase 12
   - Real-device QA pass (already in Phase 12 scope; this phase's matrix + FPS UAT note are the inputs).
   - OG image renderer + favicon (Phase 12 scope).
   - v1.0.0 tag (Phase 12 final step).
   ```

4. Update `.planning/ROADMAP.md` — mark Phase 11 plans complete (check the boxes for 11-01/02/03 if they exist as plan list items per the planner's ROADMAP-update step).

5. Update `.planning/REQUIREMENTS.md` traceability table — change MP4-04..06 + A11Y-01..08 rows from "Pending" to "Done" (or the convention used in the file — read first).

6. Update `.planning/STATE.md` Deferred Items table — add a row for MP4-04/05 cut from v1 (referencing Phase 11). Read the existing table format first; match the style.
  </action>
  <verify>
    <automated>grep -q 'MP4-04' TODOS.md && grep -q 'MP4-05' TODOS.md && grep -q 'MP4 cut from v1' .planning/TODOS.md && test -f .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md && grep -q 'Branch D' .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md && grep -q 'MP4-04.*cut\|cut.*MP4-04' .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md && grep -q 'MP4-05.*cut\|cut.*MP4-05' .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md && grep -q 'MP4-06' .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'MP4-04' TODOS.md` outputs at least `1`
- `grep -c 'MP4-05' TODOS.md` outputs at least `1`
- `grep -c 'cut from v1' TODOS.md` outputs at least `1`
- `grep -c 'MP4 cut from v1' .planning/TODOS.md` outputs at least `1`
- `grep -q 'iOS globe projection' .planning/TODOS.md` (Phase 7 deferred item #2 carried)
- `test -f .planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md`
- 11-SUMMARY.md mentions: Branch D, MP4-04, MP4-05, MP4-06, all 8 A11Y requirements (regex: `grep -cE 'A11Y-0[1-8]' .planning/phases/11-*/11-SUMMARY.md` outputs at least `8`)
- 11-SUMMARY.md references both prior plan summaries: `grep -cE '11-0[12]-SUMMARY' .planning/phases/11-*/11-SUMMARY.md` outputs at least `2`
- 11-SUMMARY.md has the Phase 7 deferred items disposition section: `grep -q 'Phase 7' .planning/phases/11-*/11-SUMMARY.md`
- REQUIREMENTS.md traceability updated: `grep -E 'MP4-04..06.*Done|MP4-04..06.*Complete' .planning/REQUIREMENTS.md` matches OR per-row Done updates land
- STATE.md Deferred Items table has an MP4 cut row: `grep -q 'MP4.*cut.*Phase 11' .planning/STATE.md`
  </acceptance_criteria>
  <done>
MP4-04 + MP4-05 entered as v2 backlog in TODOS.md + .planning/TODOS.md; MP4-06 satisfied by 11-SUMMARY.md narrative recording Branch D selection per D-LOCK. 11-SUMMARY.md rolls up all A11Y/PERF/F8 closures + Phase 7 deferred items disposition + FPS UAT note + carry-forwards to Phase 12. REQUIREMENTS.md + STATE.md + ROADMAP.md reflect phase complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright iPhone emulation vs. real iPhone | Emulation is NOT a substitute for real-device QA per RESEARCH.md Pitfall 7 (different graphics stacks). Documented limitation in INDEX.md. |
| docs/visual-review/*.png in git | PNG screenshots may contain authenticated user content if the test stubs auth — must NOT include real PII or secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | I (Information Disclosure) | Visual-review PNGs in git containing auth tokens / PII | mitigate | `/app/*` screenshots either use a test account (no real PII) or capture the logged-out / Auth0 redirect splash. Task 1 records the chosen path. No real users' photos/emails appear in screenshots committed to repo. |
| T-11-10 | T (Tampering) | INDEX.md / 11-SUMMARY.md being a load-bearing requirement satisfaction (MP4-06) | accept | These markdown files are version-controlled; tampering is detectable via git history. The requirement says "cut path documented" — the cut path IS documented. |
| T-11-11 | R (Repudiation) | Branch D selection without explicit user sign-off | mitigate | Task 2 (checkpoint:human-verify) requires explicit user confirmation of verdicts AND the cut narrative before phase closes. |

Note: All technical attack surfaces (axe-core supply chain, FPS hook leak, suggestHandle XSS) are covered in 11-01 and 11-02 threat models.
</threat_model>

<verification>
- 10 PNG screenshots on disk.
- INDEX.md verdicts filled (post-checkpoint).
- TODOS.md + .planning/TODOS.md updated with MP4 cut entries + deferrals.
- 11-SUMMARY.md exists and rolls up all closures.
- REQUIREMENTS.md + STATE.md reflect phase-complete state.
- Human checkpoint (Task 2) confirms verdicts.
</verification>

<success_criteria>
- Visual-review matrix produced (10 PNGs + verdict INDEX.md).
- MP4-04/05/06 documented as cut from v1 in SUMMARY + v2 backlog.
- Phase 11 SUMMARY rollup exists and is internally consistent with 11-01 + 11-02 SUMMARYs.
- All 8 A11Y requirements + MP4-04/05/06 marked done in REQUIREMENTS.md.
- Phase 7 deferred mobile UAT items dispositions recorded (1 closed in-phase, 1 deferred to v1.1, 1 already addressed).
- User confirms verdicts + cut narrative.
</success_criteria>

<output>
After completion, the phase-level summary lives at `.planning/phases/11-mp4-rung-2-3-or-mobile-polish-a11y-audit/11-SUMMARY.md`.

Carry-forwards for Phase 12 (real-device QA + OG image + v1.0.0 tag):
- This phase's visual-review matrix + FPS UAT note are the inputs to Phase 12's real-device QA.
- Lighthouse baseline at docs/lighthouse/v1.0.0-baseline.json is the regression target for v1.1+.
- iOS globe projection rendering (Phase 7 #2) carries to v1.1 — NOT a Phase 12 blocker.
</output>
