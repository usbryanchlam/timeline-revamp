---
phase: 02-reel-polish
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - src/reel/MapCanvas.tsx
  - src/data/seeded-cities.ts
  - .env.example
  - README.md
autonomous: false
requirements:
  - REEL-06
user_setup:
  - "Sign up at maptiler.com (free tier, 100k requests/month). Create an API key. Paste it into .env.local as VITE_MAPTILER_KEY=<key>."
must_haves:
  truths:
    - "MapLibre canvas renders MapTiler vector-tile detail (streets, place labels) at chapter zooms 12-13"
    - "All 10 seeded chapters land on a recognizable city-block view, not a solid color"
    - "Camera arrival is visibly more cinematic than W1 demotiles"
  artifacts:
    - "src/reel/MapCanvas.tsx imports VITE_MAPTILER_KEY and constructs the style URL"
    - ".env.example documents the VITE_MAPTILER_KEY variable"
  key_links:
    - "seeded-cities.ts zoom values restored to 11-13.5 range matching the W1 originals (see git show fcd06ea -- src/data/seeded-cities.ts)"
    - "MapCanvas style URL switches from demotiles to MapTiler"
---

<objective>
Swap the W1 placeholder demotiles tile source for MapTiler vector tiles so the camera lands on cinematic city-block detail. Restore the seed-data zoom levels that were temporarily lowered to work around demotiles' world-only data.

Purpose: Deliver REEL-06 ("MapTiler vector tiles wired; zooms restored to 12-13 with cinematic city-block landings"). Without this, REEL-04 (cinematic flyTo curves) cannot be subjectively tuned because the camera lands on solid color.

Output: MapCanvas points at MapTiler, seed data uses zoom 11-13.5, .env.example documents the key, README explains setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

@src/reel/MapCanvas.tsx
@src/data/seeded-cities.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pre-execution check — VITE_MAPTILER_KEY is the only blocker to running this plan autonomously.</what-built>
  <how-to-verify>Confirm the user has signed up at maptiler.com, created an API key, and added `VITE_MAPTILER_KEY=<key>` to `.env.local` at the repo root.</how-to-verify>
  <resume-signal>Type "key in .env.local" to continue, or "skip" if you want to use the demotiles fallback (this plan will exit early).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Document the env var in .env.example</name>
  <files>.env.example</files>
  <read_first>.gitignore</read_first>
  <action>Create `.env.example` at the repo root if it does not exist. Add a section documenting VITE_MAPTILER_KEY with a comment pointing at maptiler.com and noting the free-tier limit (100k/mo). The file is committed; .gitignore already excludes `.env.local` and `.env.*.local` (verify line "!.env.example" is present so the example file IS tracked). Do not put any real keys in this file — placeholder only.</action>
  <verify>cat .env.example and confirm the file documents `VITE_MAPTILER_KEY=` with a comment.</verify>
  <acceptance_criteria>
    - ".env.example exists at repo root"
    - ".env.example contains the literal string 'VITE_MAPTILER_KEY='"
    - ".env.example contains a comment mentioning maptiler.com"
    - ".gitignore line '!.env.example' is present (so .env.example is tracked)"
  </acceptance_criteria>
  <done>.env.example committed with VITE_MAPTILER_KEY documented; .env.local stays gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Switch MapCanvas to MapTiler style URL</name>
  <files>src/reel/MapCanvas.tsx</files>
  <read_first>src/reel/MapCanvas.tsx</read_first>
  <action>Replace the `DEMOTILES_STYLE` constant. Construct the style URL from the env var: `const MAPTILER_KEY = import.meta.env.VITE_MAPTILER_KEY` and `const STYLE_URL = MAPTILER_KEY ? \`https://api.maptiler.com/maps/streets-v2-dark/style.json?key=${MAPTILER_KEY}\` : 'https://demotiles.maplibre.org/style.json'`. Use `streets-v2-dark` because the design is dark-mode-first; the dark variant matches the navy bg-map token. Update the `new maplibregl.Map({ style: STYLE_URL, ... })` call to use the new constant. If the key is missing at runtime, log a clear warning to console exactly once and fall back to demotiles so dev work doesn't break. Remove or rename the `DEMOTILES_STYLE` constant since it's no longer the primary.</action>
  <verify>bun run typecheck && bun run dev — open localhost:5173, confirm map shows street labels and roads at the first chapter (Tokyo).</verify>
  <acceptance_criteria>
    - "src/reel/MapCanvas.tsx contains the literal 'import.meta.env.VITE_MAPTILER_KEY'"
    - "src/reel/MapCanvas.tsx references 'streets-v2-dark' as the style name"
    - "src/reel/MapCanvas.tsx still references 'demotiles.maplibre.org' as a fallback path"
    - "bun run typecheck exits 0"
  </acceptance_criteria>
  <done>MapCanvas dynamically picks MapTiler when key is present, demotiles otherwise. Type-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restore seeded zoom levels to W1 originals</name>
  <files>src/data/seeded-cities.ts</files>
  <read_first>src/data/seeded-cities.ts</read_first>
  <action>For each of the 10 cities, restore the zoom + pitch + bearing values that existed before commit fcd06ea ("fix: bottom-horizontal ChapterRail + lower zooms for demotiles"). Reference values: Tokyo (zoom 12.5, pitch 55), Kyoto (13, 50), Seoul (12, 48), Reykjavik (11, 60), Lisbon (13, 55), Marrakech (13.5, 50), Cape Town (11.5, 58), Queenstown (12, 62), Cusco (13, 55), Banff (11, 65). Bearings stay as currently set (visual variety). Update the zoom-note comment block at the top of the file to reflect: "zoom 11-13.5, MapTiler vector tiles render city-block detail". Remove the W2-deferred note about MapTiler.</action>
  <verify>git diff src/data/seeded-cities.ts shows zoom values restored. bun run dev — flyTo chapters cycle through cities at the higher zoom.</verify>
  <acceptance_criteria>
    - "src/data/seeded-cities.ts contains 'zoom: 12.5' (Tokyo)"
    - "src/data/seeded-cities.ts contains 'zoom: 13.5' (Marrakech, the highest)"
    - "src/data/seeded-cities.ts contains 'zoom: 11' for at least one entry (Reykjavik or Banff)"
    - "src/data/seeded-cities.ts no longer contains 'zoom: 4' or 'zoom: 4.5'"
    - "Top-of-file comment no longer says 'W2 swaps to MapTiler'"
  </acceptance_criteria>
  <done>Seed cities at production zoom band, comment block reflects current state.</done>
</task>

<task type="auto">
  <name>Task 4: Update README setup instructions</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>Add a one-paragraph subsection under `## Develop` titled `### Map tiles`. Explain: by default, dev falls back to demotiles (gray placeholders); for cinematic detail, sign up at maptiler.com (free 100k/mo), create a key, and put `VITE_MAPTILER_KEY=<key>` in `.env.local`. Keep it terse — three sentences maximum.</action>
  <verify>cat README.md | grep -A 5 "Map tiles"</verify>
  <acceptance_criteria>
    - "README.md contains the heading '### Map tiles'"
    - "README.md mentions 'maptiler.com' and 'VITE_MAPTILER_KEY'"
    - "README.md mentions the demotiles fallback"
  </acceptance_criteria>
  <done>Onboarding instructions explicit; future you (or a teammate) won't wonder what tile source is supposed to be live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MapCanvas swap complete — dev server should now show MapTiler streets-v2-dark tiles at city zoom for all 10 chapters.</what-built>
  <how-to-verify>Run `bun run dev`, open http://localhost:5173 (or the LAN IP), let auto-play cycle through 3 chapters. Verify: (a) you see real streets and place labels, not solid color polygons; (b) the dark-mode style matches the navy app chrome; (c) no console warnings about missing keys. Test on the iPhone too if convenient — same behavior expected.</how-to-verify>
  <resume-signal>Type "tiles look right" to mark complete, or describe any visual issues for follow-up.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run typecheck passes
- [ ] bun run build passes
- [ ] Dev server renders MapTiler tiles at all 10 chapters (manual check)
- [ ] Falling back to demotiles when key absent does not error (manual check: comment out key, restart, observe console warning + fallback)
- [ ] No real key committed to git: `git log -p .env.example | grep -E '[A-Za-z0-9]{20,}'` returns nothing
</verification>

<success_criteria>

- All tasks completed and checkpoints approved
- Manual visual review on dev server confirms cinematic city detail (REEL-06 satisfied)
- .env.example documents the variable; README documents the setup
- Tile source switch is reversible (fallback path preserved for dev convenience)
</success_criteria>
