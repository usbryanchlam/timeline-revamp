---
phase: 08-deploy-part-1
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - infra/DEPLOY.md
  - .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md
autonomous: false
gap_closure: false
requirements:
  - DEPLOY-05
tags:
  - phase-08
  - deploy
  - dns
  - uat
  - smoke-test
  - phase-07-uat-closure
user_setup:
  - service: dns-bryanlam-dev
    why: "DNS cutover for timeline.bryanlam.dev requires operator access to the bryanlam.dev zone (registrar / DNS provider unknown -- depends on the user's account)."
    env_vars: []
    dashboard_config:
      - task: "Lower the existing TTL on the bryanlam.dev zone to 300s at least 24 hours BEFORE cutover (so a botched flip propagates back quickly)."
        location: "DNS provider dashboard for bryanlam.dev"
      - task: "Update the A record for `timeline.bryanlam.dev` to point at the OCI VM's Reserved Public IP. Type=A, Name=timeline, Value=<vm-ip>, TTL=300."
        location: "DNS provider dashboard for bryanlam.dev"
  - service: auth0
    why: "Production callback URL must be whitelisted before the first authenticated login on the public domain."
    env_vars: []
    dashboard_config:
      - task: "Add https://timeline.bryanlam.dev/app to Allowed Callback URLs (already has dev URLs)."
        location: "Auth0 dashboard -> Applications -> Timeline SPA -> Settings -> Allowed Callback URLs"
      - task: "Add https://timeline.bryanlam.dev to Allowed Logout URLs and Allowed Web Origins."
        location: "Auth0 dashboard -> Applications -> Timeline SPA -> Settings"
  - service: oci-object-storage
    why: "Verify the timeline-photos bucket's CORS policy permits the production origin. Per memory feedback_oci_cors_via_s3.md, OCI's Console UI lacks a CORS tab and the Native API silently drops corsRules -- only S3-compat API works."
    env_vars: []
    dashboard_config:
      - task: "Verify CORS via S3-compat API: aws s3api get-bucket-cors --bucket timeline-photos --endpoint-url https://<namespace>.compat.objectstorage.<region>.oraclecloud.com -- AllowedOrigins must include 'https://timeline.bryanlam.dev'."
        location: "Local laptop with aws CLI configured against OCI S3-compat endpoint"
      - task: "If missing, re-apply via S3-compat PutBucketCors (NOT OCI Console, NOT Native API)."
        location: "Local laptop"

must_haves:
  truths:
    - "DNS A record for timeline.bryanlam.dev resolves to the OCI VM's Reserved Public IP from at least two independent resolvers (1.1.1.1 + 8.8.8.8)."
    - "https://timeline.bryanlam.dev/api/health returns 200 + {status:'ok', db:'ok'} via the public hostname (no curl --resolve trickery)."
    - "openssl s_client against the public hostname shows a valid Let's Encrypt chain with future expiry."
    - "sudo certbot renew --dry-run still succeeds AFTER the DNS cutover (no regression vs. 08-02)."
    - "Auth0 production callback https://timeline.bryanlam.dev/app is whitelisted -- first authenticated login succeeds end-to-end."
    - "OCI Object Storage CORS permits https://timeline.bryanlam.dev (verified via S3-compat API per memory)."
    - "All 3 deferred Phase 7 mobile UAT items pass on a real iPhone 14 Pro / iOS 17+ Safari, closing the Phase 7 UAT debt: 60 FPS orbit, GlobeReel 3D projection, mixed-case URL resolves + Nginx X-Cache-Status MISS->HIT."
    - "07-HUMAN-UAT.md summary updates from 1/4 passed, 3 pending -> 4/4 passed."
  artifacts:
    - path: "infra/DEPLOY.md"
      provides: "Two new sections appended: ## DNS Cutover (steps 1-7) and ## Smoke Test (bare automated battery + 3 mobile UAT items + Auth0 + OCI CORS verifications)."
    - path: ".planning/phases/07-public-urls-handle/07-HUMAN-UAT.md"
      provides: "Updated test results for items 1, 2, 3 (pending -> pass) + summary recomputed (1/4 -> 4/4)."
  key_links:
    - from: "DNS A record timeline.bryanlam.dev"
      to: "OCI VM Reserved Public IP"
      via: "DNS provider's A-record edit"
      pattern: "A\\s+timeline"
    - from: "https://timeline.bryanlam.dev/api/health"
      to: "Nginx :443 -> API loopback :8787 -> Postgres SELECT 1"
      via: "end-to-end public smoke"
      pattern: "https://timeline\\.bryanlam\\.dev"
    - from: "07-HUMAN-UAT.md items 1/2/3"
      to: "live deployed surface on iPhone Safari"
      via: "in-person UAT walkthrough with Web Inspector"
---

<objective>
Flip DNS for `timeline.bryanlam.dev` to the OCI VM and execute the smoke battery that closes Phase 7's deferred mobile UAT items. This plan turns the pre-DNS TLS pre-flight from 08-02 into a public, browser-reachable HTTPS endpoint, then verifies on a real iPhone that the cinematic motion + globe projection + case-insensitive URL handling all work on the live deployed stack.

Purpose: DEPLOY-05 (DNS cutover) is the last remaining requirement of Phase 8. The smoke battery (D-16) is the gate at which Phase 7's three pending UAT items close -- 07-HUMAN-UAT.md goes from 1/4 to 4/4 passed. This is also the only point where photos load over the production Auth0 + OCI CORS path under real network conditions, so we exercise both as part of the smoke.

Output: Two appended sections in `infra/DEPLOY.md` (DNS Cutover + Smoke Test) + an updated `07-HUMAN-UAT.md` summary. The plan is `autonomous: false` because the mobile UAT items require a human + a real iPhone -- they cannot be automated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<background>
This plan closes the locked decisions in `08-CONTEXT.md` for DNS + smoke:

- **D-19** Cutover sequencing: (1) verify TLS works via curl --resolve against VM IP, (2) flip DNS, (3) wait for propagation, (4) run smoke battery. 08-02 already covered step 1; this plan owns steps 2-4.
- **D-16** Smoke = bare automated checks + 3 deferred Phase 7 mobile UAT items. Items spelled out verbatim in 08-CONTEXT.md.

`07-HUMAN-UAT.md` currently reads `total: 4, passed: 1, pending: 3` -- this plan flips items 1/2/3 from `pending` to `pass` once the iPhone walkthrough succeeds on the live deployed stack. Item 4 (HandlePickerModal cannot be dismissed) already passed during Phase 7 via the 216a0cd fix.

The OCI Object Storage CORS verification step is load-bearing per RESEARCH `Runtime State Inventory` row + `Common Pitfalls` reference to `feedback_oci_cors_via_s3.md`: photos may CORS-block on production even though dev works, and the only reliable verification path is the AWS CLI against OCI's S3-compat endpoint (NOT OCI Console UI, NOT Native API). The smoke section in DEPLOY.md documents this explicitly.

This plan is mostly documentation + operator orchestration. The two file edits are `infra/DEPLOY.md` (appends) and `07-HUMAN-UAT.md` (status updates). The bulk of the work is operator-driven on the VM, in browsers, and on a real iPhone -- captured here as explicit numbered checkpoints.
</background>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-deploy-part-1/08-CONTEXT.md
@.planning/phases/08-deploy-part-1/08-RESEARCH.md
@.planning/phases/08-deploy-part-1/08-VALIDATION.md
@.planning/phases/08-deploy-part-1/08-01-PLAN.md
@.planning/phases/08-deploy-part-1/08-02-PLAN.md
@.planning/phases/07-public-urls-handle/07-HUMAN-UAT.md
@infra/DEPLOY.md
@ops/nginx/timeline.conf
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ## DNS Cutover + ## Smoke Test sections to infra/DEPLOY.md</name>
  <files>
    - infra/DEPLOY.md (MODIFIED -- two new sections appended after the Nginx + Let's Encrypt section from 08-02 Task 3)
  </files>
  <read_first>
    - infra/DEPLOY.md (the version from 08-02 -- append after the Nginx + Let's Encrypt section, BEFORE Common Operations)
    - .planning/phases/08-deploy-part-1/08-CONTEXT.md D-16 (smoke verbatim items), D-19 (cutover sequencing)
    - .planning/phases/08-deploy-part-1/08-RESEARCH.md "Runtime State Inventory" (Auth0 + OCI CORS verification rows), "Common Pitfalls" 5 + 6 (iptables + DNS-before-cert), Example 5 (wait-for-healthy retry loop)
    - .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md (full file -- this section's mobile UAT items mirror items 1/2/3 verbatim)
    - .planning/phases/07-public-urls-handle/07-CONTEXT.md D-21 (per-URL cache key -- mixed-case URL test depends on this)
    - ~/.claude/projects/-Users-bryanlam-Workspaces-timeline-revamp/memory/feedback_oci_cors_via_s3.md (OCI Console UI / Native API both silently drop CORS rules; only S3-compat API works)
  </read_first>
  <action>
    Append two new sections to `infra/DEPLOY.md`. Insert AFTER the "Nginx + Let's Encrypt" section, BEFORE the "Common Operations" section.

    **Section 1: ## DNS Cutover** (~50 lines, numbered checklist)

    ```markdown
    ## DNS Cutover

    Pre-flight requirement: 08-02's `curl --resolve` smoke MUST return HTTP/2 200 BEFORE running these steps. If it does not, fix Nginx / certbot first -- this section assumes a valid cert is on the VM (D-19).

    ### Pre-cutover (24 hours ahead)

    1. **Lower TTL** on the existing DNS zone for `bryanlam.dev` to 300 seconds. This ensures a botched cutover propagates back within ~5 minutes instead of hours. Skip if TTL is already 300s or lower. Verify with:
       ```bash
       dig +short timeline.bryanlam.dev @1.1.1.1
       dig +noall +answer timeline.bryanlam.dev @1.1.1.1
       ```

    ### Cutover

    2. **Verify the VM IP is a Reserved Public IP** (not a dynamic ephemeral IP). On the OCI Console: Networking -> Reserved Public IPs -> confirm the IP attached to the VM's VNIC has `Lifecycle State: Available` and is `Assigned` to the primary VNIC. CLI alternative:
       ```bash
       oci network public-ip get --public-ip-id <ocid> --query 'data.{state:"lifecycle-state",assigned:"assigned-entity-id"}'
       ```
       If the VM is on a dynamic IP, attach a reserved IP NOW (free on OCI free tier) before continuing -- otherwise the IP changes on next stop/start and DNS breaks.

    3. **Pre-flight TLS smoke from laptop** (last check before flipping DNS):
       ```bash
       VM_IP=<vm-ip>
       curl --resolve timeline.bryanlam.dev:443:$VM_IP -fI https://timeline.bryanlam.dev/api/health
       openssl s_client -connect $VM_IP:443 -servername timeline.bryanlam.dev </dev/null 2>/dev/null \
         | openssl x509 -noout -dates -issuer
       ```
       Expected: 200 + valid LE chain. If not, STOP -- 08-02 is not finished.

    4. **Flip the DNS A record** at the bryanlam.dev DNS provider:
       - Type: `A`
       - Name: `timeline` (or `timeline.bryanlam.dev` depending on the provider's UI convention)
       - Value: the VM's reserved public IP
       - TTL: 300

    5. **Wait for propagation + verify from multiple resolvers:**
       ```bash
       dig +short timeline.bryanlam.dev @1.1.1.1
       dig +short timeline.bryanlam.dev @8.8.8.8
       dig +short timeline.bryanlam.dev @9.9.9.9
       ```
       All three should return the VM IP. If one resolver lags, wait another 1-2 minutes (300s TTL means ~5 min max).

    6. **Update Auth0 production callback URL** (RESEARCH Runtime State Inventory):
       - Auth0 dashboard -> Applications -> Timeline SPA -> Settings
       - Allowed Callback URLs: add `https://timeline.bryanlam.dev/app`
       - Allowed Logout URLs: add `https://timeline.bryanlam.dev`
       - Allowed Web Origins: add `https://timeline.bryanlam.dev`
       - Save changes.

    7. **Verify OCI Object Storage CORS** (per memory feedback_oci_cors_via_s3.md -- OCI Console UI has no CORS tab; the Native API silently drops corsRules):
       ```bash
       aws s3api get-bucket-cors \
         --bucket timeline-photos \
         --endpoint-url https://<namespace>.compat.objectstorage.<region>.oraclecloud.com
       ```
       `AllowedOrigins` MUST include `https://timeline.bryanlam.dev`. If missing, re-apply via S3-compat `put-bucket-cors` (NOT OCI Console UI, NOT Native API -- both silently drop).
    ```

    **Section 2: ## Smoke Test** (~80 lines, three subsections)

    ```markdown
    ## Smoke Test

    Closes DEPLOY-05 + the 3 deferred Phase 7 mobile UAT items per D-16. After this section's six gates pass, Phase 8 is done.

    ### Bare automated battery

    Run from the laptop (NOT on the VM -- the public hostname must resolve via DNS):

    1. **Health endpoint reachable via public hostname:**
       ```bash
       curl -fsS https://timeline.bryanlam.dev/api/health | jq
       ```
       Expected output:
       ```json
       {"status":"ok","db":"ok"}
       ```

    2. **TLS chain valid + future expiry:**
       ```bash
       openssl s_client -connect timeline.bryanlam.dev:443 -servername timeline.bryanlam.dev </dev/null 2>/dev/null \
         | openssl x509 -noout -dates -issuer -subject
       ```
       Expected: `issuer=...Let's Encrypt...`, `notAfter=<date ~90 days out>`.

    3. **Renewal still works post-DNS:**
       ```bash
       ssh ubuntu@<vm> 'sudo certbot renew --dry-run'
       ```
       Expected: exit 0 + "Congratulations, all simulated renewals succeeded".

    4. **certbot.timer still active:**
       ```bash
       ssh ubuntu@<vm> 'systemctl is-active certbot.timer && systemctl is-enabled certbot.timer'
       ```
       Expected: `active\nenabled\n`.

    5. **HTTP -> HTTPS redirect works:**
       ```bash
       curl -fI http://timeline.bryanlam.dev/api/health
       ```
       Expected: `HTTP/1.1 301 Moved Permanently`, `Location: https://timeline.bryanlam.dev/api/health`.

    6. **First authenticated login round-trip succeeds:**
       Open `https://timeline.bryanlam.dev/app` in a browser. Click Sign In. Auth0 Universal Login renders. After login, browser lands back on `https://timeline.bryanlam.dev/app` with a valid session. (If Auth0 redirects to an error page, the callback URL whitelist from DNS Cutover step 6 is missing or wrong.)

    ### Mobile UAT (real iPhone 14 Pro, iOS 17+, Safari)

    These three items close Phase 7's UAT debt (07-HUMAN-UAT.md). Use a real iPhone -- the device-specific GPU + WebGL + globe-projection rendering paths cannot be simulated. Connect the phone to a Mac via USB for Web Inspector access.

    7. **UAT-1: iPhone Safari sustains 60 FPS on 1-city OrbitReel for 30s+:**
       - Setup: stage a single-city test handle (e.g., `bryanlam-test-1city`) with exactly one city. If no such handle exists, sign in on the laptop, add one city, log out, and use that handle.
       - Visit `https://timeline.bryanlam.dev/u/<1-city-handle>` on iPhone Safari.
       - On Mac: Safari -> Develop -> <iPhone Name> -> select the page tab.
       - Open Web Inspector -> Timelines tab -> Rendering Frames -> Start.
       - Observe for 30+ seconds.
       - Pass criterion: all (or essentially all -- transient spikes okay during the very first second of orbit start are acceptable) bars stay BELOW the 16.67ms (60 FPS) target line.

    8. **UAT-2: GlobeReel renders as actual 3D globe on iOS Safari:**
       - Setup: stage a 0-city handle (no cities).
       - Visit `https://timeline.bryanlam.dev/u/<0-city-handle>` on iPhone Safari.
       - Pass criterion: visually confirm the globe is SPHERICAL -- continents curve toward the poles (not a flat mercator). The slow rotation should be visible.

    9. **UAT-3: Mixed-case URL `/u/Bryan` resolves same as `/u/bryan` + Nginx per-URL cache (D-21):**
       - Setup: use the existing handle (e.g., `bryan`).
       - Visit `https://timeline.bryanlam.dev/u/Bryan` on iPhone Safari -> same reel renders.
       - Visit `https://timeline.bryanlam.dev/u/bryan` on iPhone Safari -> same reel renders.
       - From laptop, verify Nginx per-URL caching:
         ```bash
         # First request -- expect MISS (cold cache):
         curl -sI https://timeline.bryanlam.dev/u/Bryan | grep -iE 'X-Cache-Status'
         # Wait 1-2 seconds, second request -- expect HIT:
         curl -sI https://timeline.bryanlam.dev/u/Bryan | grep -iE 'X-Cache-Status'
         ```
         First should print `x-cache-status: MISS`; second `x-cache-status: HIT`. The intentional per-URL cache key (`$scheme$host$uri`) means /u/Bryan and /u/bryan are SEPARATE cache entries -- both should serve the same content, both should MISS-then-HIT independently.
       - Pass criterion: both URLs render the same reel; X-Cache-Status flips MISS -> HIT on the second request to the same URL.

    ### Photos load over CORS

    10. **Real photo load smoke (catches the RESEARCH Pitfall 3 ESM/require regression + Pitfall 2 VITE_* env regression + OCI CORS regression):**
        - Visit `https://timeline.bryanlam.dev/u/<handle-with-photos>` from iPhone Safari.
        - Scroll through the reel.
        - Pass criterion: photos appear in the chapter overlays (no broken-image icons; no CORS errors in iPhone Safari Web Inspector -> Console).

    After ALL TEN gates pass, Phase 8 is complete. Update `.planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` to mark items 1/2/3 as `pass`.
    ```
  </action>
  <acceptance_criteria>
    - infra/DEPLOY.md gains both sections: `grep -cE '^## DNS Cutover' infra/DEPLOY.md` returns 1; `grep -cE '^## Smoke Test' infra/DEPLOY.md` returns 1.
    - Section ordering preserved: in `infra/DEPLOY.md`, line number of `## DNS Cutover` is GREATER than `## Nginx + Let's Encrypt` AND LESS than `## Common Operations`. Verify: `awk '/^## Nginx \+ Let.?s Encrypt/{n=NR} /^## DNS Cutover/{d=NR} /^## Smoke Test/{s=NR} /^## Common Operations/{c=NR} END{exit !(n<d && d<s && s<c)}' infra/DEPLOY.md` exits 0.
    - TTL lowering documented: `grep -E 'TTL.*300|300.*TTL' infra/DEPLOY.md` exits 0.
    - Reserved IP verification documented: `grep -iE 'reserved.*public.*ip|public.*ip.*reserved' infra/DEPLOY.md` exits 0.
    - Multi-resolver dig check: `grep -cE 'dig.*@(1\.1\.1\.1|8\.8\.8\.8|9\.9\.9\.9)' infra/DEPLOY.md` returns >= 3 (three separate dig commands across resolvers).
    - Auth0 callback whitelist documented: `grep -E 'https://timeline\.bryanlam\.dev/app' infra/DEPLOY.md` exits 0; `grep -iE 'allowed callback' infra/DEPLOY.md` exits 0.
    - OCI CORS landmine called out: `grep -iE 's3-compat|s3 compat' infra/DEPLOY.md` exits 0; `grep -iE 'silently drop|silent.*drop' infra/DEPLOY.md` exits 0.
    - Bare smoke checks documented (the 6 automated bare gates): `grep -cE 'curl -fsS https://timeline\.bryanlam\.dev/api/health' infra/DEPLOY.md` returns >= 1; `grep -E 'openssl s_client -connect timeline\.bryanlam\.dev:443' infra/DEPLOY.md` exits 0; `grep -E 'certbot renew --dry-run' infra/DEPLOY.md` exits 0; `grep -E 'is-active certbot\.timer' infra/DEPLOY.md` exits 0; `grep -E '301 Moved Permanently' infra/DEPLOY.md` exits 0.
    - Three mobile UAT items documented verbatim: `grep -cE 'UAT-1|UAT-2|UAT-3' infra/DEPLOY.md` returns >= 3.
    - 60 FPS gate documented: `grep -E '60 FPS|16\.67ms' infra/DEPLOY.md` exits 0.
    - X-Cache-Status MISS -> HIT documented: `grep -iE 'X-Cache-Status' infra/DEPLOY.md` exits 0; `grep -iE 'MISS.*HIT|miss.*hit' infra/DEPLOY.md` exits 0.
    - Photo CORS smoke documented: `grep -iE 'broken.*image|cors.*error' infra/DEPLOY.md` exits 0.
  </acceptance_criteria>
  <deviation_handling>
    If the DNS provider for `bryanlam.dev` is Cloudflare (or similar) with proxy mode enabled, ALSO disable the proxy ("DNS only" / gray cloud) -- proxied mode would re-terminate TLS upstream of the VM and break the Let's Encrypt chain. Document this branch in the DNS Cutover section if discovered. If the iPhone test cannot achieve sustained 60 FPS due to thermal throttling on an older device, document the device model + observed FPS in the SUMMARY and flag for Phase 12 polish (not a Phase 8 blocker -- 07-HUMAN-UAT.md item 1's expected criterion is "iPhone 14 Pro" specifically).
  </deviation_handling>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (manual): Execute the DNS Cutover + 10-step Smoke Test from DEPLOY.md</name>
  <what-built>
    Task 1 committed the runbook. This checkpoint is the actual execution: operator runs the 7-step DNS Cutover section then the 10-step Smoke Test section on the live deployed stack. Until all 10 smoke gates pass, the plan is not complete.
  </what-built>
  <how-to-verify>
    Follow `infra/DEPLOY.md` ## DNS Cutover (steps 1-7) then ## Smoke Test (gates 1-10) IN ORDER. For each gate, record:

    - **Gate 1 (curl /api/health):** the JSON body + HTTP status.
    - **Gate 2 (openssl TLS chain):** issuer, notAfter date, subject.
    - **Gate 3 (certbot renew --dry-run):** the final "Congratulations" line.
    - **Gate 4 (certbot.timer):** `active`, `enabled`.
    - **Gate 5 (HTTP 301 redirect):** the Location header.
    - **Gate 6 (Auth0 login round-trip):** screenshot or note of successful return to /app with logged-in state.
    - **Gate 7 (UAT-1 iPhone 60 FPS):** screenshot of Web Inspector Rendering Frames showing bars below 16.67ms line; device model + iOS version.
    - **Gate 8 (UAT-2 globe projection):** screenshot of the spherical globe on iPhone.
    - **Gate 9 (UAT-3 mixed-case + X-Cache-Status):** both curl -sI outputs (the MISS and the HIT lines).
    - **Gate 10 (photos load):** confirmation that the chapter overlays show photos (not broken-image icons).

    If ANY gate fails, STOP and report which gate + the exact failure. Do not proceed to Task 3 until all 10 pass.
  </how-to-verify>
  <resume-signal>
    Type **"approved"** if all 10 smoke gates pass. Include in the resume message: the timestamp of cutover, the DNS resolver results from DNS Cutover step 5, the cert expiry from Gate 2, and one-line confirmation per UAT gate (e.g., "UAT-1: pass, iPhone 14 Pro iOS 17.2, 0 frames over 16.67ms across 32s observation"). Type **"failed at gate N: <exact failure>"** if any gate fails -- record which gate + what was observed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update 07-HUMAN-UAT.md: items 1/2/3 pending -> pass + summary 1/4 -> 4/4</name>
  <files>
    - .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md (MODIFIED)
  </files>
  <read_first>
    - .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md (full file, 45 lines -- understand the existing summary format: `total: N`, `passed: N`, `pending: N`, etc.)
    - The Task 2 resume message (operator-supplied observations) -- these become the `result:` text for each UAT item.
  </read_first>
  <action>
    Update `.planning/phases/07-public-urls-handle/07-HUMAN-UAT.md`:

    1. **Update frontmatter:** Change `status: partial` to `status: complete` and bump `updated:` to the current ISO-8601 timestamp.

    2. **For each of the three items (1, 2, 3):** Replace the `result: [pending]` line with a real result paragraph mirroring the existing item 4 format. Use the operator's Task 2 resume-message observations verbatim where possible. Each result paragraph should be a single line starting with `pass (Phase 8 deploy smoke 08-03 -- <timestamp>): <observation>`.

       Example for item 1:
       ```
       result: pass (Phase 8 deploy smoke 08-03 -- 2026-05-15T22:30:00Z): iPhone 14 Pro iOS 17.2 Safari on https://timeline.bryanlam.dev/u/<handle>, Web Inspector Timelines Rendering Frames observed 32s on OrbitReel, all bars below 16.67ms line.
       ```

       Example for item 2:
       ```
       result: pass (Phase 8 deploy smoke 08-03 -- 2026-05-15T22:35:00Z): iPhone 14 Pro iOS 17.2 Safari on https://timeline.bryanlam.dev/u/<0-city-handle>, continents visibly curve toward poles, slow rotation observed.
       ```

       Example for item 3:
       ```
       result: pass (Phase 8 deploy smoke 08-03 -- 2026-05-15T22:40:00Z): iPhone Safari on https://timeline.bryanlam.dev/u/Bryan and /u/bryan both render the same reel; from laptop curl -sI shows x-cache-status: MISS then HIT on second request per-URL (D-21 per-URL cache key working as designed).
       ```

       Substitute the actual operator-observed timestamp + device + observation from the resume message. Do NOT leave placeholder `<handle>` etc -- use the real values the operator reports.

    3. **Update summary block:** Change
       ```
       total: 4
       passed: 1
       issues: 0
       pending: 3
       skipped: 0
       blocked: 0
       ```
       to
       ```
       total: 4
       passed: 4
       issues: 0
       pending: 0
       skipped: 0
       blocked: 0
       ```

    4. **Clear the `## Current Test` block:** Replace `[awaiting human testing]` with `[all items complete]`.

    Do NOT modify item 4's `result:` line (already pass).
  </action>
  <acceptance_criteria>
    - Frontmatter status updated: `grep -E '^status: complete' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` exits 0.
    - All three items report pass: `grep -cE '^result: pass \(Phase 8' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` returns 3 (items 1, 2, 3).
    - Item 4's original pass note preserved: `grep -E '216a0cd' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` exits 0 (item 4's pass marker references the Phase 7 fix commit).
    - No more pending: `grep -cE '\[pending\]' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` returns 0.
    - Summary block updated: `grep -E '^passed: 4$' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` exits 0; `grep -E '^pending: 0$' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` exits 0.
    - Current Test block updated: `grep -E '\[all items complete\]' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` exits 0.
  </acceptance_criteria>
  <deviation_handling>
    If the operator's Task 2 resume message reports a partial failure (e.g., UAT-1 observed 5 frames over 16.67ms across the 30s window), do NOT silently mark it pass. Instead:
    - Mark the item `result: partial (Phase 8 deploy smoke 08-03 -- <timestamp>): <observed>; <followup>`.
    - Bump `passed:` by 2 (not 3) and add a `partial: 1` line in the summary block.
    - Log a `## Followups` entry at the bottom of the file noting which item and what the next step is (e.g., "Phase 12 polish: revisit OrbitReel choreography on older iPhone models").
    - The PHASE is still effectively done for v1 purposes if the gates are met within reasonable tolerances; the planner's judgment in setting strict 16.67ms criterion accepts minor real-world variance. The operator + planner agree on whether to mark partial vs pass.
  </deviation_handling>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> DNS resolver -> A record -> VM IP | The cutover changes the trust anchor for the public domain. Pre-flight `curl --resolve` (08-02) verifies the cert before any third party can route to it. |
| Auth0 SPA flow -> https://timeline.bryanlam.dev/app callback | Auth0 strictly whitelists callback URLs; the production callback is added in this plan's DNS Cutover step 6. |
| Browser -> OCI Object Storage (cross-origin photo fetch) | OCI bucket CORS rules permit `https://timeline.bryanlam.dev` (verified in DNS Cutover step 7). Per memory, only the S3-compat API can verify/apply CORS correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Spoofing | DNS flipped before cert is valid -> visitors get TLS warnings + may downgrade or skip | mitigate | D-19 sequencing: 08-02 already issued the cert and verified via `curl --resolve` BEFORE 08-03 touches DNS. DNS Cutover step 3 re-verifies pre-flight. |
| T-08-17 | Spoofing | Stale Auth0 callback whitelist allows a phishing redirect | mitigate | DNS Cutover step 6 explicitly adds the production callback. Auth0's whitelist is strict (mismatched callback -> Auth0 rejects the auth response). |
| T-08-18 | Information Disclosure | OCI bucket CORS missing -> browser blocks photo loads (Information not disclosed but UX breaks) | mitigate | DNS Cutover step 7 verifies via S3-compat API; memory documents the OCI Console / Native API silent-drop trap. |
| T-08-19 | Denial of Service | DNS TTL too high -> botched cutover takes hours to propagate back | mitigate | Pre-cutover step 1 lowers TTL to 300s 24h ahead; multi-resolver verification confirms propagation in step 5. |
| T-08-20 | Tampering | Mobile UAT 3 (mixed-case URLs) fails because case-collision creates inconsistent results | mitigate | Phase 7 D-21 per-URL cache key is intentional (at-most-2x cache space); both URLs render the same app-layer content via LOWER() lookup. UAT-3 explicitly tests this. |

</threat_model>

<verification>
After all three tasks land:

1. **Static checks (repo working tree):** all Task 1 + Task 3 acceptance greps pass.
2. **Public hostname reachability:** `curl -fsS https://timeline.bryanlam.dev/api/health` returns 200 + `{"status":"ok","db":"ok"}`.
3. **DNS resolves consistently across resolvers:** at least 2 of {1.1.1.1, 8.8.8.8, 9.9.9.9} return the VM IP.
4. **Cert + renewal still healthy post-cutover:** Smoke Test gates 2-4 pass.
5. **Auth0 production login round-trip works:** Smoke Test gate 6 passes.
6. **All 3 mobile UAT items report pass in 07-HUMAN-UAT.md:** `grep -c '^result: pass' .planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` returns 4.
7. **OCI CORS verified via S3-compat API:** DNS Cutover step 7 reports `AllowedOrigins` includes the production domain.
8. **Photos visibly load on iPhone Safari on the deployed stack:** Smoke Test gate 10.

If gates 1-5 or 10 fail, this is an infrastructure regression and must be fixed before Phase 8 ships. If a mobile UAT item (gates 7-9) reports a marginal failure, the deviation_handling block in Task 3 applies.
</verification>

<success_criteria>
Phase 8 ships when:
- All three task `<acceptance_criteria>` blocks pass.
- The operator's resume message from Task 2 confirms all 10 smoke gates passed (or that any partial result has been logged as a Phase 12 followup per Task 3 deviation handling).
- `.planning/phases/07-public-urls-handle/07-HUMAN-UAT.md` reports `passed: 4, pending: 0`.
- ROADMAP.md's Phase 8 row gets checked off (out-of-scope for this plan -- happens at `/gsd-verify-work` / phase-complete step).
- 08-03-SUMMARY.md documents the cutover timestamp, the cert expiry post-cutover, the 10 smoke-gate outcomes (with values, not just pass/fail), the device used for mobile UAT, and any followups identified for Phase 9 polish or Phase 12 launch polish.

Post-08-03, `https://timeline.bryanlam.dev` is the live production URL of the portfolio. Phase 9 (CI/CD + tag-trigger auto-deploy) is the natural next phase.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-part-1/08-03-SUMMARY.md` using the standard summary template. Include:
- Files modified (list)
- Cutover timestamp + DNS provider used + observed propagation time
- All 10 smoke gate outcomes with concrete values (HTTP status, cert expiry, FPS observation, X-Cache-Status flip, photo load confirmation)
- Mobile UAT device + iOS version
- Confirmation of Auth0 callback whitelist + OCI CORS allow-list state
- Total Phase 8 elapsed time (from 08-01 first commit to this point)
- Followups identified for Phase 9 (CI/CD, rate limiting, HSTS / security headers polish) and Phase 12 (any UAT partial results that need real-device polish)
- Confirmation that Phase 7's 3 deferred UAT items are now closed (07-HUMAN-UAT.md 4/4)
</output>
