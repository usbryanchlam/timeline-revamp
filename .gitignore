# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Playwright generated artifacts (per-run results, HTML reports, traces)
test-results/
playwright-report/

# Env
.env
.env.*
!.env.example

# OCI API key material (PEM files; never commit)
.oci/
*.pem

# Postgres data (if ever bind-mounted; named volume `pgdata` is Docker-managed)
pgdata/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Testing / coverage
coverage/
.nyc_output/

# Caches
.cache/
.parcel-cache/
.turbo/
.vercel/

# gstack scratch
.gstack/

# Local progress checkpoints (int-compact output)
progress*.md

# Git worktrees (superpowers skill)
.worktrees/

# Local dev TLS certs from mkcert (machine + LAN-specific; never commit)
.dev/certs/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
!.terraform.lock.hcl
tfplan
crash.log
crash.*.log
