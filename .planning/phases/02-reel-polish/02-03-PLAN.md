---
phase: 02-reel-polish
plan: 03
type: feature
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - package.json
  - bun.lock
  - src/reel/ChapterOverlay.tsx
  - src/reel/MapCanvas.tsx
  - src/motion/variants.ts
autonomous: false
requirements:
  - REEL-04
  - REEL-05
  - REEL-07
user_setup: []
must_haves:
  truths:
    - "Photo stack fades in offset-by-stagger when a chapter lands, not hard-snap"
    - "City name and caption arrive with subtle staggered timing using --ease-arrival"
    - "Camera flyTo curves feel cinematic on real MapTiler tiles (subjective pass on Apple-Weather-feel)"
    - "Arrival pulse animation runs on the active chapter pin (currently exists; verify)"
  artifacts:
    - "package.json includes framer-motion ^11"
    - "src/motion/variants.ts exports reusable motion variants (photoStack, cityName, caption)"
    - "src/reel/ChapterOverlay.tsx uses Framer Motion <motion.div> for the photo stack and text"
  key_links:
    - "Motion variants reference the CSS custom-property easings (--ease-arrival, --ease-ui) so DESIGN.md tokens stay the source of truth"
    - "MapCanvas flyTo curve and duration tuned to feel cinematic; values match DESIGN.md motion table"
---

<objective>
Replace hard-snap photo overlays with Framer Motion 11 staggered choreography. Tune MapLibre flyTo curve + duration values now that real MapTiler tiles render. Verify the existing arrival-pulse animation still fires correctly on the chapter overlay.

Purpose: Deliver REEL-04 (cinematic flyTo curves), REEL-05 (Framer-staggered photo overlays), REEL-07 (arrival pulse on land). These are the subjective polish items that take the reel from "demo" to "production-grade."

Output: Framer Motion installed, motion variants module, ChapterOverlay re-implemented with motion components, MapCanvas curve/duration tuned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DESIGN.md

@src/reel/ChapterOverlay.tsx
@src/reel/MapCanvas.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install framer-motion 11</name>
  <files>package.json, bun.lock</files>
  <action>Run `bun add framer-motion@^11`. Add it as a regular dependency, not devDependency (motion runs at runtime). Verify the version installed is >= 11.0.0.</action>
  <verify>bun pm ls | grep framer-motion</verify>
  <acceptance_criteria>
    - "package.json dependencies include 'framer-motion' with version starting '^11'"
    - "bun.lock contains framer-motion entry"
  </acceptance_criteria>
  <done>Framer Motion 11 installed and locked.</done>
</task>

<task type="auto">
  <name>Task 2: Define reusable motion variants</name>
  <files>src/motion/variants.ts</files>
  <read_first>DESIGN.md, src/index.css</read_first>
  <action>Create `src/motion/variants.ts`. Export three variants for the chapter overlay choreography. Each uses `transition.ease` matching the design system's CSS custom-property easings (Framer accepts an array `[0.16, 1, 0.3, 1]` directly, mirroring `--ease-arrival`). The variants:

  1. `photoStackContainer` — has `staggerChildren: 0.08` so each photo fades+rises in offset by 80ms.
  2. `photoStackItem` — `hidden: { opacity: 0, y: 12, scale: 0.96 }`, `visible: { opacity: 1, y: 0, scale: 1, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } }`.
  3. `cityNameAndCaption` — `hidden: { opacity: 0, y: 8 }`, `visible: { opacity: 1, y: 0, transition: { duration: 0.4, ease: [0.4, 0, 0.2, 1], delay: 0.15 } }` (--ease-ui curve, slightly delayed so type arrives after photos start).

  All durations and curves are explicit numeric values (Framer needs literals; CSS custom properties are not directly supported here). DESIGN.md tokens stay the source of truth — the literals here are derived from those tokens, with a comment block at the top of the file naming each token by name.</action>
  <verify>bun run typecheck</verify>
  <acceptance_criteria>
    - "src/motion/variants.ts exports 'photoStackContainer', 'photoStackItem', 'cityNameAndCaption'"
    - "src/motion/variants.ts contains the cubic-bezier coefficients [0.16, 1, 0.3, 1] for arrival easing"
    - "src/motion/variants.ts has a comment block referencing the matching DESIGN.md tokens (--ease-arrival, --ease-ui)"
  </acceptance_criteria>
  <done>Variants module is the single source of truth for motion choreography; no inline magic numbers in components.</done>
</task>

<task type="auto">
  <name>Task 3: Convert ChapterOverlay to Framer motion components</name>
  <files>src/reel/ChapterOverlay.tsx</files>
  <read_first>src/reel/ChapterOverlay.tsx, src/motion/variants.ts</read_first>
  <action>Replace the existing static photo `<div>`s with `<motion.div variants={photoStackItem} />` wrapped in a `<motion.div variants={photoStackContainer} initial="hidden" animate="visible" />` parent. Replace the existing `.animate-arrival` CSS class on each photo with the variant-driven motion. Keep the rotation transforms (the editorial polaroid feel) — apply them via `style={{ rotate: i === 0 ? -4 : 6 }}`. Keep the gradient backgrounds, alt text, role.

  Convert the city name `<h1>` and caption `<p>` to `<motion.h1>` and `<motion.p>` using `cityNameAndCaption` variant. Drop the `.animate-arrival` className on the h1 (Framer is now driving it). The chapter counter line stays as static markup.

  Critical: keep the parent `<div className="pointer-events-none ...">` and the scrim/padding wrapper unchanged. Only the inner content is converted to motion primitives. The component still keys on `chapter.id` upstream (in Reel.tsx) — that key change re-mounts the whole tree, which restarts variants from "hidden" → "visible" on every chapter change. That's the trigger for the pulse.</action>
  <verify>bun run typecheck && bun run dev — observe photos staggering in (you should see the second photo fade in ~80ms after the first), and the city name arriving slightly after the photos.</verify>
  <acceptance_criteria>
    - "src/reel/ChapterOverlay.tsx imports from 'framer-motion'"
    - "src/reel/ChapterOverlay.tsx imports variants from '@/motion/variants'"
    - "src/reel/ChapterOverlay.tsx contains '<motion.div' (at least 2 instances)"
    - "src/reel/ChapterOverlay.tsx no longer references the className 'animate-arrival' (now driven by Framer, not CSS keyframes)"
  </acceptance_criteria>
  <done>Chapter overlay choreography is variant-driven, staggered, observably smoother than W1.</done>
</task>

<task type="auto">
  <name>Task 4: Tune flyTo curve + duration in MapCanvas</name>
  <files>src/reel/MapCanvas.tsx</files>
  <read_first>src/reel/MapCanvas.tsx</read_first>
  <action>Adjust the camera tuning constants. Change `ARRIVAL_CURVE = 1.42` to `ARRIVAL_CURVE = 1.6` (slightly more zoom-out arc for the inter-continental jumps; reads as more cinematic). Change `FLY_DURATION_MS = 1400` to `FLY_DURATION_MS = 1800` (gives the eye time to register the arrival, matches Apple-Weather-pace). Keep the `easeArrival` function as-is. Also update the import at the top of the file: there's a constant `FLY_DURATION_MS = 1400` that needs to match the same name imported by useGestureMachine.ts (which uses it for the CHAPTER_FLY_DONE timer). To stay consistent: update `src/gestures/stateMachine.ts` `FLY_DURATION_MS` from 1400 to 1800 too. Verify by grep — both files should report 1800.</action>
  <verify>grep -n "FLY_DURATION_MS" src/reel/MapCanvas.tsx src/gestures/stateMachine.ts</verify>
  <acceptance_criteria>
    - "src/reel/MapCanvas.tsx contains 'FLY_DURATION_MS = 1800' OR 'FLY_DURATION_MS: 1800'"
    - "src/reel/MapCanvas.tsx contains 'ARRIVAL_CURVE = 1.6'"
    - "src/gestures/stateMachine.ts contains 'FLY_DURATION_MS = 1800'"
  </acceptance_criteria>
  <done>Camera lands slightly slower with a more pronounced arc; gesture machine's CHAPTER_FLY_DONE timer matches the new duration so state stays in sync.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Motion polish complete: staggered photo overlay, tuned flyTo, all using DESIGN.md motion tokens.</what-built>
  <how-to-verify>1) `bun run dev` on iPhone. 2) Watch one full auto-play loop. Verify: photos fade in offset-stagger (not all at once); city name arrives just after the photos start; camera flyTo feels like Apple Maps Flyover, not jerky; no animation runs on the prefers-reduced-motion path (test by toggling iOS Settings → Accessibility → Reduce Motion). 3) Subjective Apple-Weather-pace pass: does it feel "premium app polish" rather than "demo"?</how-to-verify>
  <resume-signal>Type "feels right" or "tweak [X]" with one specific tuning ask.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run typecheck passes
- [ ] bun run build passes (Framer added 30-50 KB gzipped to motion chunk; main chunk should still meet PERF-02 from 02-02)
- [ ] Manual: photo stack stagger visible on every chapter advance
- [ ] Manual: prefers-reduced-motion path skips Framer animations (Framer respects the media query by default)
- [ ] Manual: state machine's CHAPTER_FLY_DONE timer fires within ~50ms of the actual flyTo finishing (no orphan SCRUBBING/CHAPTER_SWIPE state)
</verification>

<success_criteria>

- All tasks completed and final visual review approved
- REEL-04, REEL-05, REEL-07 satisfied
- DESIGN.md motion tokens still authoritative — variants reference them by name in comments
- Build size delta from Framer Motion is acceptable (< 60 KB gzipped added to motion chunk; verify in build output)
</success_criteria>
